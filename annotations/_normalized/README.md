This folder is generated by `scripts/annotations/normalize_external_annotations.py`.

Outputs:
- `BRCA_immune_subtypes_advanced.normalized.tsv`
- `Thornsson_immune_table.normalized.tsv`
- `HLA_types.normalized.tsv`
- `RNA_expression.sample_metadata.tsv` (one row per RNA expression column header)
- `ATAC_case_level.sample_metadata.tsv` (one row per ATAC matrix column header)

Each normalized *tidy* table includes:
- `participant`, `sample`, `sample_vial`, `aliquot`

Run:
```bash
source .venv/bin/activate
python scripts/annotations/normalize_external_annotations.py
```

