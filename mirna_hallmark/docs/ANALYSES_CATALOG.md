# Analyses catalog — `mirna_hallmark`

One row per analysis component. This catalog is **local** to the subproject and
independent of `analysis/` catalogs. CLI is always
`.venv/bin/python3 -m mirna_hallmark.<module>` from the repo root.

> **Formulas:** every pressure / share / specificity / coupling / family /
> validation-priority equation is defined in detail in
> [`docs/FORMULAS.md`](FORMULAS.md). Read it before interpreting any pressure or
> share number.

## Orientation — where the code lives (read this before scanning the table)

*(Folded in from the former `MODULE_MAP.md`. Tree + module lists **re-verified 2026-07-17**;
per-folder counts are as measured on disk today, not as originally reported.)*

The subproject was de-sprawled in the **2026-07-04 reorg: top level went 160 → 36 `.py`**
(35 modules + `__init__`). All finished one-off analyses/figures and their theme-base utilities
moved into a themed `analyses/` + `eval/` tree; **the top level now holds only the data/infra
spine, the frozen baseline heuristic, and `run_all`'s live pipeline steps.** So a bare component
name in the table below (e.g. `outcome_advanced`) is generally **not** top-level — find it in the
tree, and **run it by its new dotted path**:
`.venv/bin/python3 -m mirna_hallmark.analyses.outcome.outcome_advanced`.

```
mirna_hallmark/
  <35 top-level .py: SPINE + BASELINE + pipeline + run_all — see below>
  analyses/                    # finished one-off analyses + their theme bases (OFF the build path)
    dcis_ev/      (20)         # DCIS + EV arc: loaders, CAF/NF, mir29c, ev_mirna_* screens/deep/followup
    misc/         (17)         # cross-theme one-offs (comovement, promiscuity, methylation, escape, …)
    outcome/      (13)         # survival/prognostic: outcome_survival base + outcome_* + prognostic
    cnv_locus/    (12)         # miRNA-locus CN/SV, CCLE, dosage attribution
    pressure_dev/ (12)         # pressure heuristic sensitivity/comparison/validation/prognostic
    spatial/      (10)         # spatial_common base + visium/xenium/mibi/deconv
    cptac/         (8)         # CPTAC one-offs (orphan discovery/confound, acquired pressure, …)
    evidence_dev/  (8)         # ENCORI/evidence-scoring sensitivity + comparison + fetch
    edge_panels/   (7)         # edge pressure/corr panels + edge_prior_refinement + edge_breast_context
    cross_state/   (4)         # cross_state_landscape/coupling/deep_dive/expression_panels bases
    nmf/           (4)         # nmf_* + within_* signatures
    builders/      (3)         # _build_depmap_dependency / _build_tf_census / _build_meth_chunked (cache)
    figures/       (2)         # make_grant_figure / make_seed_grant_figures
    mir301/        (2)         # mir301_family_depth / mir301_focus_genes
    ops/           (2)         # rerun_* batch scripts
    architecture/  (1)         # geneset_architecture
  eval/           (15)         # reused OOD/validation: coupling_permutation, held_out_tuning,
                               #   core_coupling_{deconv,composition}_retest, buffa/cptac_validation,
                               #   targetscan_orphan_coupling, decoy_bench, …
  learned/                     # THE LEARNED MODEL (canonical estimator) — modules + outputs are
                               #   catalogued in LEARNED_MODEL_DISCOVERY_SYNTHESIS.md §7
  method_dev/                  # organized subpackage (predates the reorg)
```

### The 35 modules that stayed top-level

Rule: a module stays only if it is **shared infra the learned model / eval will reuse**, the
**frozen baseline heuristic**, or a **live `run_all` pipeline step**. Nothing else.

- **SPINE — data/infra, REUSE (21).** `config` `data_loaders` `stats` `hallmark_sets`
  `mirna_arm_resolve` `healthy_anchor` `gtex_mirna_matrix` `arm_expression` `estimate_scores`
  `brca_deconvolution` `mirna_locus_cnv` `gene_roles` `robustness_checks` `coupling_inference`
  `build_edges` `evidence_scoring` `encori_edges` `seed_family_justification`
  `family_normal_reference` `tcga_batch` `cptac_batch`.
  *Evidence-refactor targets* (→ the PMID-deduped, method-centric ledger): `build_edges` (emit
  ledger not counts), `evidence_scoring` (superseded by the learned fusion), `encori_edges`.
- **BASELINE — frozen heuristic, do NOT extend (8).** `pressure_build` `pressure_engine`
  `ago_gate` `hallmark_interaction` `mirna_state_class` `hybrid_pressure` `dual_spine_comparison`
  `pressure_signature_curated_info`. The learned model must *nest and beat/match* these.
- **PIPELINE — live `run_all` steps (5) + orchestrator.** `coupling_predictor_comparison`
  `pam50_gene_resolution` `stratum_characterization` `subtype_contrasts`
  `visibility_archetype_contrasts`; and **`run_all`** (the only true full-graph leaf; drives the
  pipeline).

## Output lanes (do not mix)

| Lane | Path | What belongs here |
|------|------|-------------------|
| **Core Hallmark spine** | `output/{edges,ago_gate,hallmark_interaction,...}` | TCGA primary-tumor Hallmark pressure, strata, `run_all` |
| **Tissue reference** | `output/tissue_reference/` | TCGA tumor / NAT / GTEx cross-state context, UMAP, miR-301 family depth (tissue) |
| **Plasma EV (optional)** | `output/plasma_ev/` | Circulating exosome miRNA GEO cohorts only (`ev_mirna_*.py`); **not** in `run_all` |

Tissue-reference modules must **not** write under `plasma_ev/`. The plasma-EV lane is
translational validation against TCGA tissue; keep it isolated unless you are
explicitly bridging the two (e.g. optional EV contrast tables in `mir301_family_depth`).

| component_id | module | purpose | key inputs | key outputs |
|--------------|--------|---------|-----------|-------------|
| `hallmark_sets` | `hallmark_sets.py` | parse + index the 50 Hallmark sets | `annotations/GSEA/h.all.v2026.1.Hs.txt` | in-memory `HallmarkSets` |
| `build_edges` | `build_edges.py` | vectorized Hallmark-scoped miRTarBase summary + annotated edges | miRTarBase raw, family info, Hallmark universe | `edges/mirtarbase/mirtar_interaction_summary.csv`, `edges/mirna_hallmark_edges.tsv.gz` |
| `ago_gate` | `ago_gate.py` | per-sample RISC capacity + bounded gate; AGO expr/CNV by stratum | RNA log2TPM, ASCAT3 CNV, clinical | `ago_gate/per_sample_ago_capacity.tsv.gz`, `ago_expression_by_stratum.tsv`, `ago_cnv_by_stratum.tsv` |
| `mirna_locus_cnv` | `mirna_locus_cnv.py` | miRNA-universe (locus/arm/family) CNV by stratum + CN→expr concordance | ASCAT3 CNV, miRNA loci, miRNA expr, clinical | `mirna_locus_cnv/mirna_cnv_by_stratum.tsv`, `mirna_cnv_expr_concordance.tsv`, `tables/sample_entity_cnv.tsv.gz` (cache) |
| `mirna_cnv_genome_maps` | `mirna_cnv_genome_maps.py` | segment-based miRNA locus genome map + MIMAT paralog weighted healthy reference (`sum(2*w_locus)`) + interesting-case flags | `mirna_locus_cnv/tables/sample_entity_cnv.tsv.gz`, `data/miRNA/cnv_miRNA.csv`, `locus_mimat_weight_reference.tsv` | `mirna_locus_cnv/maps/mirna_cnv_locus_genome_map.tsv`, `mirna_cnv_mimat_paralog_map.tsv`, `mirna_cnv_mimat_summary.tsv`, `mirna_cnv_interesting_cases.tsv`, PAM50 PNG maps |
| `mirna_cnv_subtype_depth` | `mirna_cnv_subtype_depth.py` | focus-miRNA CN distributions by PAM50, subtype comparisons, same-segment / +/-2 Mb neighborhood co-alteration | cached segment-based miRNA CNV, locus catalog, MIMAT weights, PAM50 strata | `mirna_locus_cnv/maps/mirna_cnv_focus_subtype_distribution.tsv`, `mirna_cnv_focus_subtype_comparison.tsv`, `mirna_cnv_focus_pairwise_subtype.tsv`, `mirna_cnv_neighborhood_by_subtype.tsv` |
| `target_combined_anticorr` | `target_combined_anticorr.py` | per-target-gene combined miRNA pressure anti-correlation (**M0** `softmax_z_logrpm` + `evidence_mass`; 1,349 genes tested) with gated/ungated cohort partials and within-PAM50 raw + partial (CPE/HRD/target-CN) | Hallmark miRTarBase summary, miRNA arms, RNA, AGO gate, target-gene CNV, clinical | `target_combined_anticorr/target_combined_anticorr.tsv`, `target_combined_anticorr_by_pam50.tsv`, `target_combined_top_mirnas.tsv` |
| `coupling_predictor_comparison` | `coupling_predictor_comparison.py` | **Does the pressure *construction* change realized coupling vs raw abundance?** (MH-44; review-driven, wired into `run_all` step 10). Same CPE+HRD+target-CN partial Spearman as the spine, BH-FDR per (level,predictor), ungated. **Edge** level (5,101 edges): predictor ∈ {abundance, z, softmax, softmax_logrpm, softmax_z, softmax_z_logrpm} (reuses `compute_edge_pressure_map`; edge weight constant → rank-invisible). **Gene** level (1,408 genes): predictor ∈ {abundance_sum (naive Σ logrpm), softmax, softmax_logrpm, softmax_z, softmax_z_logrpm} (reuses `compute_gene_pressure`). Summary: neg-sig counts, sign-concordance / ρ-rankcorr / neg-sig recovery vs the abundance baseline. Finding: `z`≈abundance, the **softmax share** is the sole differentiator — it *weakens* single-edge coupling (abundance most sensitive) but the full gene pressure *beats* naive abundance-sum (+8% neg-sig). **Conditioning sweep (E4, MH-78):** also fixes the predictor at raw abundance and varies the co-regulator CONDITIONING over the edge universe — `none` / `precision` (semi-partial: resid Y only on `P_others`) / `attribution` (resid both); `P_others=gene_total−c` (spine field, = `decoupling_validation` block 6). Finding: conditioning does **not** beat abundance (precision 1,471→1,369, attribution →1,390, median Δρ≈0.0001) — the competitive field is not a target-noise source. **Logit grid (`--logit-grid`, MH-78):** bare share under reference{cohort/healthy/NAT/paired-NAT}×measure{rawx/dev/z}×promiscuity vs abundance — best ~906≪1,471; paired-NAT=low-power noise. **Function/occupancy grid (`--function-grid`):** softmax/temperature/sparsemax/linear-L1/**mass-action occupancy** + AGO-gated dose (full/partial/purified) + stricter-evidence prune; mass-action closest (~90% under precision) but loses, AGO-gating hurts (proliferation confound). Verdict: raw ungated abundance most sensitive across all 6 edge axes. **GENE construction grid (`--gene-grid`, MH-79):** `partial-ρ(P_agg, y_g)` over construction (function × reference {cohort/healthy/NAT/rawx} × promiscuity, Σ share·z·logrpm) × prune {all/evid-tertile/evid-decile/abund-floor/topk} × AGO-gating {ungated/full/partial/purified} vs `abundance_sum`; + role/coherence overlay. Finding: edge lemma FLIPS — **NAT-referenced share +12.6% over abundance_sum** (reference is the winning axis; gating hurts; pruning neutral-to-harmful). Construction primitives now in `pressure_engine` (shared). `--max-genes` caps the universe (debug); `--no-conditioning` skips the E4 sweep | Hallmark miRTarBase edges, miRNA arms, RNA, target-gene CNV, clinical (CPE+HRD), GTEx/NAT/matched-NAT + AGO/proliferation (deep grids) | `coupling_predictor_comparison/{edge_coupling_by_predictor.tsv.gz,gene_coupling_by_predictor.tsv.gz,predictor_comparison_summary.tsv,edge_conditioning_*,edge_logit_grid_*,edge_function_grid_*,method_manifest.json}` |
| `cn_dosage_attribution` | `cn_dosage_attribution.py` | **end-to-end CN→regulatory-network attribution (MH-43).** (A) per-arm split of dosage into **CN-driven vs CN-residual** pressure (spine, AGO-gated) → how much of target repression rides CN; (B) **combined multi-locus CN-load granularity contest** (single_best / locus / chrom / cluster). Cohort + PAM50/immune/TNBC; every CN coupling reported **baseline (CPE+HRD+target-CN) and broad-aneuploidy-adjusted** (`thornsson_aneuploidy_score`). `--max-genes` smoke, `--no-gate` | arm-level + locus-level miRNA CNV cache (`mirna_locus_cnv/tables/sample_entity_cnv.tsv.gz`), locus genome + cluster maps, miRNA arms, RNA, AGO gate, target-gene CNV, clinical (+aneuploidy) | `cn_dosage_attribution/{cn_attribution_cohort,cn_attribution_by_context,cn_attribution_summary,cn_load_granularity_cohort,cn_load_granularity_summary}.tsv`, `method_manifest.json` |
| `pressure_attribution_validation` | `pressure_attribution_validation.py` | **Does pressure matter over naive abundance-sum?** (MH-80). Pure join over existing outputs (no heavy compute): per gene, the pressure-vs-abundance coupling boost (MH-79 grid: spine `softmax_z_logrpm` + `shr\|softmax\|nat` vs `abundance_sum` at all/ungated), the structural-share dominance flip (`struct_vs_abundance_resolution`), per-edge functional literature (total PMIDs/evidence on surfaced vs masked arm, from `edge_breast_context/per_target_arm_reranking`), gene net-repression (`gene_corepression`), role. `canonical_revealed` = net-repressed AND pressure-boosted AND dominant-flipped AND surfaced-arm ≥1 PMID. **`--robustness`** re-tests the shortlist with ESTIMATE composition+proliferation covariates (reuses `cross_state_coupling._state_covariates`) to drop lineage/stromal artifacts (MH-51 miR-29→ECM). Result: 459 flipped → 94 canonical-revealed → **75 composition-robust**; ABCA1→miR-33b (textbook) recovered + robust. Caveat: "canonical" is a noisy automated proxy; §5a share ⊥ coupling | MH-79 gene grid comparison, `struct_vs_abundance_resolution`, `edge_breast_context` reranking, `gene_corepression`, gene roles; (`--robustness`) miRTar edges + miRNA arms + RNA + target-CN + ESTIMATE | `pressure_attribution_validation/{validated_cases.tsv,canonical_revealed.tsv,canonical_revealed_robustness.tsv,method_manifest.json}` |
| `mir301_family_depth` | `mir301_family_depth.py` | focused miR-301/130/454 family follow-up: TargetScan scores, single-arm + family-aggregate partials with **BH-FDR** (CN- and E2F/G2M+CN-adjusted `neg_sig`), cohort-wide **arm variability rule** (`low_variability` = sd<0.5 & IQR<0.75 on log2(RPM+1)), realized per-miRNA pressure shares under 5 expr variants with **four share metrics** (mean_abs / mean_pos / global_abs / global_signed), a per-gene share-metric comparison, and EV family contrasts | 301a depth outputs, TargetScan, miRNA arms, RNA, target-gene CNV, clinical, miRTar pressure edges | `tissue_reference/mir301_family_depth/family_targetscan_scores_{long,wide}.tsv`, `family_{single_arm,aggregate,all}_partials.tsv` (+q/FDR flags), `arm_variability.tsv`, `gene_mirna_pressure_contributions_by_variant.tsv`, `family_share_metric_comparison.tsv`, `family_partial_summary.tsv`, `ev_family_arm_contrasts.tsv` |
| `family_normal_reference` | `family_normal_reference.py` | family **healthy-tissue reference** contrasts: (a) TCGA tumor (01) vs tumor-adjacent-normal (NAT, 11) per-arm median log2(RPM+1), log2FC, Mann-Whitney + BH-FDR, within-family abundance rank; (b) normal-anchored absratio (tumor/NAT linear RPM); (c) descriptive raw Spearman family-arm vs focus-gene coupling in matched NAT (n~104, no covariates); (d) **GTEx v10 true-healthy breast** small-RNA-seq abundance (within-family rank/share; cross-platform so rank/sign only) + GTEx breast mRNA coupling (n~327 donors). Uses a full-barcode loader (the default subproject loader averages tumor+NAT per participant). Key result: 130a-3p is the #1-abundant family arm in **all three** references but a non-repressor in all; 454/130b are coherent repressors in healthy + tumor; 301a/301b are **tumor-induced** (≈absent in GTEx healthy) | full-barcode TCGA miRNA/RNA, `mir301_family_depth` partials, `data/GTEx/` v10 (miRNA TPM + breast mRNA TPM + small-RNA annot + sample attrs) | `tissue_reference/family_normal_reference/{family_arm_tumor_vs_nat,family_normal_anchored_absratio,family_nat_coupling,gtex_family_abundance,gtex_family_coupling,family_abundance_rank_xref}.tsv` |
| `mir301_family_network` | `mir301_family_network.py` | family **regulator-resolution network + specificity + orphan validation prioritization** on top of `mir301_family_depth`: (a) per-(gene, arm) realized-pressure share among ALL regulators (no-z `softmax_logrpm` attribution), abundance rank, realized specificity (gene-share of arm's total Hallmark target mass), and edge_class quadrant (driver / real_but_minor / structural_only / weak × specific/promiscuous); (b) arm network summary (degree, total mass, median specificity = promiscuity structure); (c) **family-as-unit pressure share per gene × PAM50** under both `softmax_z_logrpm` and `softmax_logrpm` (formula contrast); (d) **orphan validation priority** (partials-driven, orphans included via TargetScan specificity) = z-blend of coupling (FDR + prolif), TS weight, TS-specificity, PAM50 concentration × novelty weight, with PAM50-matched cell-line context | `mir301_family_depth/family_all_partials.tsv`, TargetScan context scores, miRTar pressure edges, miRNA arms, clinical | `tissue_reference/mir301_family_network/{family_arm_specificity,family_edge_master,regulator_resolution_edges,family_pressure_by_subtype,orphan_validation_priority,family_arm_network_summary,edge_class_counts}.tsv` |
| `cross_state_landscape` | `cross_state_landscape.py` | **global (non-family) cross-tissue-state landscape** over 3 refs (TCGA tumor 01 / NAT 11 / GTEx healthy breast): (1) **genome-wide miRNA arm DE** tumor-vs-NAT (same-platform median log2RPM, log2FC, Mann-Whitney + BH-FDR) with GTEx-healthy median/detection anchor and induced/suppressed/stable class; (2) **cross-state gene + Hallmark pressure** recomputed per state under the **no-z `softmax_logrpm`** attribution mode (z is within-state-standardized → ~0 in every state → not cross-state comparable; no-z magnitude is) aggregated to Hallmark programs with Δtumor-NAT / Δtumor-GTEx; (3) **miRNA pressure-budget share shift** = each arm's share of Σ\|contribution\| over the Hallmark universe per state, tumor-vs-NAT mover ranking; (4) **per-PAM50 pressure-budget share** within tumor (LumA/LumB/Her2/Basal) + dominant-subtype + share-spread. The DE table also carries a **3-state rank trajectory** (gtex→nat→tumor within-state percentiles, robust to the cross-platform scale) + a **NAT field-effect flag** (NAT already ≥halfway tumor-ward from healthy). Key results: top tumor pressure gainers = canonical oncomiRs/hypoxamiRs (miR-210/21/9/155/200c/375/196a/96), miR-301 NOT a global mover; 262 arms show NAT field-effect (adjacent-normal already tumor-like). Reuses `family_normal_reference` full-barcode + GTEx loaders | full-barcode TCGA miRNA, GTEx v10 miRNA TPM, miRTar Hallmark edges, Hallmark sets, clinical PAM50 | `tissue_reference/cross_state_landscape/{mirna_arm_de_landscape,gene_pressure_by_state,hallmark_pressure_by_state,mirna_pressure_share_shift,mirna_pressure_share_by_pam50}.tsv` |
| `cross_state_deep_dive` | `cross_state_deep_dive.py` | **per-anchor 3-state assembly** (mirror reports). `--gene G`: every miRTarBase regulator of G with, per state (tumor/NAT/GTEx), abundance (median + within-state percentile), realized pressure share on G (no-z `softmax_logrpm`), and raw-Spearman coupling; then a TargetScan-seed **family roll-up** (family share + within-family carrier arm + its coupling per state). `--mirna M`: the mirror anchored on a miRNA arm — its Hallmark targets with per-state share + coupling, the arm's abundance trajectory + its TargetScan seed-family roster (`--family` to override), and a **within-family target resolution** (for family-shared targets, which arm couples). Coupling is raw Spearman in all states (NAT/GTEx have no partials); tumor partials live in the coupling modules. Defaults: gene=PTEN, mirna=miR-200c-3p | full-barcode TCGA miRNA/RNA, GTEx v10 miRNA + breast mRNA, miRTar edges, `data/miRNA/miR_Family_Info.txt`, Hallmark sets | `tissue_reference/cross_state_deep_dive/<GENE>/{regulator_profile,family_profile}.tsv`, `mirna_<arm>/{family_arm_summary,anchor_target_profile,family_target_resolution}.tsv` |
| `estimate_scores` | `estimate_scores.py` (+ `run_estimate.R`) | **actual ESTIMATE** (Yoshihara et al. 2013) immune/stromal scores per tissue state via the canonical R `estimate` package (official 141-stromal + 141-immune signatures, package ssGSEA, platform=illumina). Runs on the FULL transcriptome of TCGA tumor / TCGA NAT / GTEx healthy breast; computed identically from each state's own expression (no platform-specific reference matrix) so it is cross-state comparable. TumorPurity intentionally omitted (affymetrix-calibrated; no NAT/GTEx analog). `--build` caches per-sample StromalScore/ImmuneScore/ESTIMATEScore; consumed by `cross_state_coupling --covariates estimate(_epi)` | full-barcode TCGA RNA, GTEx v10 breast mRNA, R `estimate` 1.0.13 | `tissue_reference/estimate/{tumor,nat,gtex}_estimate_scores.tsv` |
| `cross_state_coupling` | `cross_state_coupling.py` | **composition + proliferation-adjusted cross-state coupling trajectories.** For pressure-nominated focus edges (curated global-mover arms × top targets, or `--gene` / `--mirna`), computes per state (tumor/NAT/GTEx) the raw Spearman AND a partial Spearman adjusting for composition + proliferation — the confounders that actually apply to normal tissue (tumor purity/CPE is undefined for NAT/GTEx; SAME covariates in all states so the trajectory is comparable; tumor CN/CPE/HRD partials stay in the tumor coupling modules). `--covariates`: **estimate_epi** (default; ESTIMATE ImmuneScore+StromalScore + epithelial signature + E2F/G2M prolif), **estimate** (immune+stromal only, no epithelial axis; sensitivity audit), **metagene** (hand-curated epi/immune/stromal/prolif; sensitivity audit). Classifies each edge into a regulatory archetype (constitutive_repressor / tumor_acquired_repressor / repression_replaces_coexpr / lost_repressor / normal_coexpression_only / coexpression_all / no_consistent_signal) + a `composition_sensitive` flag. ESTIMATE resolves the immune-confounded miR-9→PTEN (NAT +0.43→+0.05); estimate_epi additionally resolves the epithelial-lineage miR-200c→ETV5/ITGB4 artifact. Validated hits robust to scheme: tumor-acquired miR-210→ISCU, miR-9→ESR1, miR-301a→PTEN/BIM, miR-17~92→PTEN; constitutive miR-429→ZEB1 | miRTar Hallmark edges, full-barcode TCGA miRNA/RNA, GTEx v10 miRNA + breast mRNA, Hallmark sets (E2F/G2M), `estimate_scores` cache | `tissue_reference/cross_state_coupling/coupling_trajectories_<focus>_<covsource>.tsv` |
| `healthy_anchor` | `healthy_anchor.py` | **healthy-anchored pressure baseline (GTEx-only).** Supplies `median_healthy(m)` so the engine's `softmax_devhealthy_logrpm` mode scores `dev = x(m,s) - median_healthy(m)` (log2, no SD division) instead of the within-tumor z. **GTEx breast (346 samples, correct tissue)**, joined by **MIMAT accession** (416 vs 395 arms by name), per-arm medians **quantile-normalised onto the TCGA scale** (rank-relative acquired elevation, cross-platform-safe). GTEx v10's uniquely-mappable pipeline (893 features) **zeros the canonical member of several seed families** (let-7a, miR-30a, miR-10a, miR-16, miR-17, miR-200b…) via multi-mapping collapse while seed-mates quantify fine — confirmed (let-7a=0 across all 346 breast samples; let-7b/g/i at 25–42k TPM). Gap filled by 2 layers then floor0: **(1 PRIMARY) anchor-calibrated miTED rank-transfer** (`source="mited_anchor"`): arms in BOTH GTEx+DIANA-miTED = anchors → monotonic miTED→GTEx QQ map (`np.interp` on sorted anchor pairs) → predict gap-arm GTEx-equiv from its miTED *rank* (platform-robust; sidesteps compositional RPM, raw slope 0.74). **Arm-specific, validated LOO vs real GTEx: Pearson 0.90, median err 0.86 log2** — preferred over seed-family which gives a degenerate family-MAX to all collapsed members (let-7a & let-7c both → let-7b; miR-103a → lone low miR-107). Fills 52 arms (let-7a→10.41, let-7c→5.80, miR-103a→4.55, miR-182 orphan→1.31). **(2 FALLBACK) seed-family** (`source="gtex_family"`, max GTEx seed-mate) only for gap arms absent from miTED — currently 0. Blood markers (miR-451a/486/92a/144…) excluded → floor0. Coverage 182→213/222 (96%) at ≥15 RPM, 87/88 at ≥255 RPM. **NAT rejected** (field-effect). Remaining orphans (no GTEx/seed-mate/miTED, or blood) stay floor0 → full acquired credit (correct for oncomiRs). `--demo` z-spine vs healthy-anchored (gainers miR-21/182/210/200c; losers let-7a/miR-125b/miR-24). Flags: `imputed`, `floor0_tumor_expressed`. **Opt-in** `--healthy-anchor` in landscape/deep-dive. Engine: `pressure_engine._dev_health_matrix` + modes `softmax_devhealthy_logrpm` etc.; config `PRESSURE_HEALTHY_ANCHOR_MODE` | full-barcode TCGA tumor miRNA, GTEx v10 breast miRNA (MIMAT), DIANA-miTED breast (anchor transfer), TargetScan seed families, Hallmark edges | `tissue_reference/healthy_anchor/{gtex_qn_baseline,expression_threshold_coverage,demo_share_zspine_vs_healthy}.tsv` |
| `mirna_state_class` | `mirna_state_class.py` | **miRNA pressure state-trajectory classification + gain/loss formalization** across GTEx-healthy→NAT→tumor, at **two resolutions**. **GTEx collapse-repair is DEFAULT** (`impute_gtex=True`; `--no-impute-gtex` audits raw): the GTEx abundance matrix has its 278 multi-mapping-collapsed arms repaired with the healthy-anchor `gtex_median` (same log2(TPM+1) scale) before contributions, so `c_gtex`/`mass_gtex`/`dHT`/`acquired_vs_gtex` are collapse-free at source (retires the let-7a/miR-30a/miR-182 artifact gainers); coupling keeps the raw GTEx matrix. (A) **EDGE (m→g)**: per-state contribution `c_X(m,g)` (no-z `softmax_logrpm`), `edge_share_tumor = c_tumor/mass_tumor(m)` (arm-side budget) **+ gene-side share `gene_share_{gtex,nat,tumor} = c_X(m,g)/Σ_reg c_X(·,g)`** (this arm's fraction of the GENE's total incoming pressure; `gene_share_rank_tumor`=1 ⇒ gene's dominant regulator; `gene_share_delta_tumor_gtex`=grip change), same-platform `edge_dNT_abs`, AND per-state composition+prolif-adjusted **partial Spearman** `adj_rho_{gtex,nat,tumor}` with **NAT separated from GTEx** (vs `cross_state_coupling` which pools them) → `coupling_archetype` + `nat_decoupled` (NAT sign-flip vs both healthy & tumor) → **`joint_edge_class`** {acquired_realized / acquired_unrealized / constitutive / lost / nat_decoupled / stable / non_monotonic / uncoupled}. Coupling on **all edges** by default (`--coupling-top-n` caps to top targets/arm; `--no-coupling` skips). (B) **miRNA (global Hallmark-universe budget)**: `mass/share/rank` per state, rank deltas **dHN** (healthy→NAT field effect) / **dNT** (NAT→tumor) / **dHT** (healthy→tumor = acquired headline; gainer=dHT>0), `primary_class` {progressive/tumor_acquired/field_effect × gain/loss, non_monotonic, stable, healthy_unknown}, **Tau** subtype-specificity `secondary_class` (pan / intermediate / subtype_specific:<sub> / subtype_excluded:<sub>) on per-PAM50 **acquired** share, flags `odd_nat_abundance` / `odd_nat_coupling` (=any nat_decoupled edge) / `baseline_source` / `imputed_baseline` / `blood` / `low_healthy_confidence`. **Magnitude acquired axis** (complements the saturating rank `dHT`): `log2fc_tumor_vs_healthy = tcga_tumor_median − healthy_baseline_tcga` (same QN'd TCGA scale, cross-platform-safe), `acquired_magnitude_class` (strong/moderate gain/loss at >2/>1 log2), combined **`acquired_gainer`** = rank-gain (dHT>0.15) OR magnitude-gain (log2fc>2), and `acquired_axis` (rank+magnitude / rank_only / **magnitude_only** = abundance surge the rank misses, e.g. miR-182/183/375/93/141/210 at +9–13 log2 with dHT≈0). 236/543 arms gain on ≥1 axis (29 rank / 187 magnitude / 20 both). Plus **edge×PAM50** breakdown (per-edge subtype pressure + edge-level Tau, tumor-only) and a **per-Hallmark-set** `set_class` secondary. `--healthy-anchor` loads the GTEx/miTED baseline for the acquired axis. Reuses `cross_state_landscape._state_matrices`, `cross_state_deep_dive._state_bundles`, `cross_state_coupling._couple_state/_state_covariates`, `healthy_anchor.load_baseline` | full-barcode TCGA miRNA/RNA, GTEx v10 miRNA+breast mRNA, miRTar Hallmark edges, Hallmark sets, clinical PAM50, ESTIMATE cache, healthy_anchor baseline | Also emits **`gene_acquired_pressure.tsv`** (gene-centric: per-gene total pressure per state, `acquired_vs_gtex` healthy-anchored + `acquired_vs_nat` collapse-free + **`acquired_vs_gtex_corrected`** = acquired minus the imputable-artifact gain, top gaining arms per gene, Hallmark membership; the floor0 gain is split by the driving arm's `baseline_source` into **`frac_gain_imputable_artifact`**, **`frac_gain_true_acquired`** (floor0 = genuinely absent in healthy), **`frac_gain_blood`**. With the GTEx collapse-repair now default, `acquired_vs_gtex` is correct at source: `frac_gain_imputable_artifact ≈ 0`, 1269/1359 genes fully clean, `acquired_vs_gtex_corrected ≈ acquired_vs_gtex` (correction now redundant); default top = real miR-21-5p program (PTPN14/IL12A/PYCR1/STRN/DOCK4); only residual floor0 gene = CANX (miR-320a)) | full-barcode TCGA miRNA/RNA, GTEx v10 miRNA+breast mRNA, miRTar Hallmark edges, Hallmark sets, clinical PAM50, ESTIMATE cache, healthy_anchor baseline | `tissue_reference/mirna_state_class/{mirna_gene_edge_class,mirna_gene_edge_class_by_pam50,mirna_state_class,mirna_state_class_by_hallmark,gene_acquired_pressure}.tsv` + `method_manifest.json` |
| `mirna_comovement` | `mirna_comovement.py` | **Stage-2 co-movement + pressure programs** (consumes `mirna_state_class` outputs). (1) **co-moving miRNAs**: arm×arm Spearman co-expression across tumor samples → average-linkage modules (`comovement_modules`/`_module_summary`) + each arm's top partners + the module's dominant trajectory/subtype class; plus an agglomerative clustering of the `(dHN,dNT)` trajectory vectors. (2) **target-set co-repression vs neutral reference** (`target_corepression`): per miRNA, a composite repression score = mean within-state z of its target set, regressed (composition+prolif-adjusted partial Spearman) on the arm's abundance per state → does the WHOLE target module move down together (realized module pressure); `delta_tumor_gtex` = acquired module repression. The z is **per-target-gene within-state** (each target standardised across that state's samples, then averaged). Parallel **edge-weighted** composite (`rho_composite_w_{state}`, weight = `evidence_score`) is shipped alongside the equal-weight one — they correlate r=0.97–0.99, i.e. weighting washes out at module scale (targets carry near-even evidence mass). **Role-stratified composite** (`rho_composite_{tsg,onco}_{state}`): the *same* partial-Spearman composite recomputed separately over the arm's TSG targets vs its oncogene targets (`malignancy_sign` from `gene_roles`, ≥4 annotated targets + ≥25 samples) — gene role is kept as a **stratification, not a reweight** (folding role into one number would re-conflate "is the module repressed" with "is the repression pro/anti-tumor"); negative `rho_composite_tsg_tumor` = coordinated repression landing on suppressors (anti-tumor realized), negative `rho_composite_onco_tumor` = on oncogenes. The module composite also serves as a **de-coupling control**: arm-wide composite collapse (miR-206) = realized-control failure vs intact module + single lost edge (miR-15a→MYB) = target-specific escape. **(2b) gene-level realized repression** (`gene_corepression.tsv`, function `gene_corepression`): gene-side mirror — per Hallmark target gene, ESTIMATE-adjusted partial-Spearman of the gene's OWN expression vs its **aggregate incoming miRNA pressure** (`compute_gene_pressure`, summed over all regulators) per state; `rho_gene_pressure_tumor<0` = gene genuinely net-repressed (280/1438; ESR1/GATA3/PGR/AURKA); `gene_repression_class` ∈ {constitutive_repressed 128, lost_repression 266, gained_repression 152, never_repressed 802} feeds `escape_scope` in `decoupling_validation`. **(2c) edge-level gene-repression scope** (`edge_gene_repression_scope.tsv`, function `edge_gene_repression_scope`): joins each edge's `joint_edge_class` against its target's `gene_repression_class` → a single direction-aware `gene_repression_scope` covering BOTH the lost side (`edge_local_escape`/`gene_wide_derepression`/`gene_not_repressed`) AND the symmetric gained side (`gene_wide_acquired_repression`/`reinforces_constitutive`/`edge_specific_acquired`). (3) **NMF pressure programs** (`nmf_factor_loadings_{mirna,gene}`, `nmf_factor_state_activity`): NMF on the non-negative miRNA×gene `c_tumor` contribution matrix → latent programs (co-regulating miRNA module + shared target module), with per-state factor activity (`shift_tumor_gtex` = programs the tumor amplifies); optional second NMF on repressive partial-coupling magnitude `max(-adj_rho,0)`. **(3-share) share NMF** (`_nmf_share` → `nmf_share_dominant`, `nmf_share_grip_change`): NMF on the regulatory-*share* matrices instead of raw pressure (the c_tumor NMF is dominated by the abundance backbone). `nmf_share_dominant` (on `gene_share_tumor`) — top factor D1 is the same backbone but secondaries diverge (loading corr ≈0 vs c_tumor NMF) and isolate **arms that *own* their few targets' budgets despite low global abundance** (D2 miR-125b/221, D3 miR-24/192, D6 miR-200 family → ETV5/ITGB4/FLNA). `nmf_share_grip_change` (signed NMF on `gene_share_tumor − gene_share_gtex`, gain/loss channels) — which arms co-GAIN vs co-LOSE regulatory grip across genes independent of pressure inflation (G1 gain = miR-21/17-3p/124 tightening on TSG/repair VHL/MSH2/TGIF1; G2 loss = miR-203a/433/92a on MAP3K1/TP63/LIFR) = the share analogue of the signed gene-pressure NMF, the only NMF reading competitive restructuring. (4) **within-tumor (per-sample) NMF** (`nmf_within_tumor_loadings_mirna`, `nmf_within_tumor_factor_subtype`): complementary to (3) — NMF on the arm×**tumor-sample** expression matrix → programs of arms that co-vary across patients (heterogeneity the aggregate `c_tumor` cannot see); each factor's per-patient activity summarised per **PAM50** (mean/frac + Kruskal-Wallis q) → subtype-specific programs, labelled with top arms (+state class) and dominant target module. Key results: epithelial miR-200/182/183/429 module (dHT +0.09), immune miR-146/150/155 module; acquired target-module repressors miR-301a/92a/25/130b (composite rho ~0→strongly neg in tumor); top tumor-amplified aggregate NMF program = miR-21/210/155/29a; within-tumor subtype programs: T3 **Basal** miR-9/17~92/210 hypoxia-oncomiR (KW q≈1e-138), T5 **LumB** miR-375/196a luminal, T8 **Her2** immune miR-142/155/146a/223. (5) **per-subtype NMF** (`nmf_subtype_persample`, `nmf_subtype_static`): NMF *inside* one PAM50 subtype — (a) per-sample on the subtype's columns, (b) "static" on the contribution matrix whose cells `c(m,g)` are recomputed from only the subtype's samples (**no sample axis**; subtype enters via the abundance multiplier). Finding: static first-factor is subtype-INVARIANT (abundance backbone), per-sample diverges (Basal_S2=miR-17~92; Her2_S2=immune). (6) **gene-pressure per-sample NMF** (`nmf_gene_pressure_loadings_gene`, `nmf_gene_pressure_factor_subtype`): NMF on the **gene×tumor-sample total-pressure** matrix (`compute_gene_pressure`) → co-pressured gene programs with per-PAM50 activity (KW), labelled with top genes + dominant Hallmark + top driver arms. Results: P4 Basal TNFA/NFKB (KW q≈3e-134, drivers miR-9/210/130b), P2 LumA EMT (miR-30/let-7), P3 LumB immune (miR-375), P7 LumA E2F (miR-10a/b). Same gene-pressure NMF is also run **within each PAM50 subtype** (columns sliced; `nmf_gene_pressure_within_subtype`) → subtype-private co-pressured gene programs (e.g. Basal_G1 adipogenesis, Her2_G1 EMT, LumB_G1 complement). (7) **acquired + signed gene-pressure NMF**: (a) **acquired** (`nmf_gene_pressure_acquired_*`) = same gene-pressure NMF but on dev-anchored pressure clipped `≥0` (gain only); (b) **signed** (`nmf_gene_pressure_signed_*`) handles **gain AND loss in one NMF** — each gene's signed dev pressure split into `<gene>|gain`/`<gene>|loss` channels, stacked (2·genes×samples) and decomposed once, direction preserved; each factor reports `direction`, `gain_mass_frac`, top gain-genes and loss-genes. Finding: all signed programs are **gain-dominated (0.967–0.992)** → no organised pressure-relief program; the acquired (gain-only) NMF was not discarding a loss signal | `mirna_state_class/{mirna_state_class,mirna_gene_edge_class}.tsv`, full-barcode TCGA miRNA/RNA + GTEx bundles, ESTIMATE cache, Hallmark sets | `tissue_reference/mirna_comovement/{comovement_modules,comovement_module_summary,target_corepression,gene_corepression,edge_gene_repression_scope,nmf_share_dominant,nmf_share_grip_change,nmf_factor_loadings_mirna,nmf_factor_loadings_gene,nmf_factor_state_activity,nmf_repressive_coupling_programs,nmf_within_tumor_loadings_mirna,nmf_within_tumor_factor_subtype,nmf_subtype_persample,nmf_subtype_static,nmf_gene_pressure_loadings_gene,nmf_gene_pressure_factor_subtype,nmf_gene_pressure_within_subtype,nmf_gene_pressure_within_subtype_loadings,nmf_gene_pressure_acquired_factor_subtype,nmf_gene_pressure_signed_factor_subtype,nmf_gene_pressure_signed_loadings}.tsv` |
| `within_subtype_signatures` | `within_subtype_signatures.py` | **Legacy wrapper** — prefer `nmf_sample_signatures`. Per-sample distribution of within-subtype gene-pressure signatures only. |
| `nmf_sample_signatures` | `nmf_sample_signatures.py` | **Per-sample signature composition for every sample-axis NMF**, each on its appropriate cohort. Refits H (factor×sample), column-normalises to composition, scores `dominant_factor`/`dominant_share`/`entropy`/`eff_n_signatures` + `call` (dominant if top≥0.50). Analyses: **cohort-wide** gene-pressure abundance (`gene_pressure_cohort`, k=12, n=1096), acquired gain (`gene_pressure_acquired_cohort`), signed ± channels (`gene_pressure_signed_cohort`), within-tumor miRNA arm abundance (`mirna_within_tumor_cohort`); **per-PAM50** gene-pressure abundance+acquired (`gene_pressure_within_{sub}`, `gene_pressure_acquired_within_{sub}`, k=12) and miRNA within-subtype (`mirna_within_{sub}`, k=8). Cohort-wide heatmaps: samples ordered by PAM50 block + dominant signature + colour bar. **Not included** (no sample axis): aggregate `c_tumor` NMF, share NMF, per-subtype static arm NMF. **Key findings:** (1) all gene-pressure views are **graded mixtures** — cohort-wide only 3–6% dominant, within-subtype 4–14%; median top-share 0.31–0.35; ~6–7 active programs/tumour. (2) Cohort-wide miRNA within-tumor NMF is flattest (**0% dominant**, median 7.1 programs). (3) Per-subtype **miRNA** NMF is modestly more discrete — Basal **26%** dominant (S4 miR-150/142 immune), LumA 16% (S3 immune-pan), vs gene-pressure Basal 4%. Basal miRNA heterogeneity partitions a minority; gene-pressure co-occurs continuously. Outputs `nmf_sample_signatures/{summary_all,per_sample/*.tsv}` + `figures/*.png`. CLI: `python -m mirna_hallmark.analyses.nmf.nmf_sample_signatures` (`--no-acquired-within` skips per-subtype acquired) | `mirna_state_class`, TCGA tumor miRNA, Hallmark sets, clinical PAM50, healthy_anchor baseline | `tissue_reference/mirna_comovement/nmf_sample_signatures/{summary_all,per_sample/*.tsv}` + `figures/*.png` |
| `within_pathway_nmf` | `within_pathway_nmf.py` | **Within-pathway NMF** — restricts the matrix to **one Hallmark set's genes** and decomposes *that* (per-set k by reconstruction-error elbow), surfacing sub-modules INSIDE a program that the cross-set NMF (`mirna_comovement`) averages away. **TWO families.** **Family A — gene×sample pressure**, 5 flavours (NOT interchangeable — the elbow gives each a different k; chosen k = the sub-structure signal): **abundance** (`softmax_logrpm`), **spine** (`softmax_z_logrpm`, the canonical M0 coupling pressure), **hybrid** (`softmax_z`+combined_mass), **acquired** (dev-anchored clip≥0), **signed** (gain\|loss). Scopes: **cohort + within-PAM50** (`--no-within-subtype` skips). Per factor: top genes, driver arms, per-PAM50 activation + KW q, `factor_label`; per (set,mode,scope): per-sample dominance. **Family B — edge-level miRNA×gene lenses** (different *matrix*, no sample axis; within-pathway analogue of `_nmf_programs`/`nmf_share_*`): **aggregate** (`c_tumor`), **share** (`gene_share_tumor`, who owns the budget), **grip_change** (signed `share_tumor−share_gtex`, competitive restructuring) — each factor pairs an arm-module with a gene-module. **Key findings:** (1) **the z-spine/hybrid are the RESOLVING lenses** (elbow k≈5, ~2.6–2.9 sub-modules) while **abundance collapses to ~one module** (k=2, eff 1.7 — the few high-abundance arms own the set's pressure); the naive "% dominant" read is k-confounded → use k_chosen/eff_n_sigs. (2) **EMT (spine, k=4)** → inflammatory-contractile (MYLK/INHBA/VEGFA/IL6, Basal q1e-109) · **collagen (miR-29a/b/c → COL4A1/SPARC/FBN1/COL5A2, pan-subtype** — recovers MH-36/38, correctly KW-non-sig per the stromal/MH-39 caveat) · mesenchymal-marker (miR-30/182); **E2F (k=5)** → Basal replication (RAD21/STMN1, miR-92a/197) vs LumA mitotic (RRM2/TOP2A, let-7) vs splicing/checkpoint. (3) within-subtype re-fit adds structure for some programs (APOPTOSIS 1.5→3.2 inside Her2; HYPOXIA →3.6 inside LumB). (4) **grip_change lens**: in EMT **miR-21/17-3p gain grip** (RHOB/TGFBI/COL4A1/VEGFA) while **miR-29a/b/c lose grip** on collagen — control handed from suppressor families to the oncomiR. CLI: `python -m mirna_hallmark.analyses.nmf.within_pathway_nmf [--sets …] [--flavours …] [--kmax 6] [--min-genes 40] [--no-within-subtype] [--no-heatmaps]`. Computes 4 full-universe pressure matrices once (row/col-sliced per set). Wired into `run_tissue_reference_full.sh` step 12. See **MH-42** | `mirna_state_class` (edge `c_tumor`/`gene_share_*`), TCGA tumor miRNA, Hallmark sets, clinical PAM50, healthy_anchor baseline | `tissue_reference/within_pathway_nmf/{factor_summary,set_dominance,lens_factors,per_sample.tsv.gz}.tsv` + `figures/*.png` |
| `geneset_architecture` | `geneset_architecture.py` | **Stage-3 (pilot) architecture-weighted gene-set pressure.** Overlays a set's regulatory **topology** onto the otherwise-flat Hallmark pressure: fetches **OmniPath** directed+signed interactions (REST via urllib, cached; no networkx — PageRank is a numpy power iteration) for a set's members, builds the induced signed subgraph, and derives per-gene weights — **master-regulator** weight = reverse-PageRank (influences many downstream), **effect_sign** (node with mostly-inhibitory out-edges = inhibitor; a miRNA repressing it de-represses the program → sign flip), **redundancy discount** (nodes sharing downstream targets, Jaccard≥0.5, split weight). Then per miRNA: `flat_pressure` (sum c_tumor over members) vs `arch_pressure` (Σ w_arch·c) vs `arch_signed_pressure` (Σ effect_sign·w_arch·c = net effect on program output; <0 = represses inhibitors → net pathway-activating), with flat→arch `rank_shift`. **Calibrated continuous edge sign** (replaces binary flatten): each ordered pair aggregates OmniPath records to `net_sign=(n_stim−n_inh)/(n_stim+n_inh)∈[−1,+1]` (dual/ambiguous edges contribute graded, not 0); per node `effect_sign_soft`=mean net_sign over out-edges (binary `effect_sign` kept for legacy `arch_signed`). **Topology propagation**: a finite-horizon signed-path (Katz, α=0.25, H=6) `output_influence(g)=1+Σ_j T[g,j]` on the **net_sign** signed adjacency → `program_output_change(m)=Σ_g −c_tumor·output_influence(g)` (>0 = repression nets to activating the program). **Tumor-direction prior** (`TUMOR_DIRECTION_PRIOR`, coarse BC polarity: +1 pro-tumor EMT/HYPOXIA/PI3K/E2F/MYC/…, −1 suppressive APOPTOSIS/P53/IFN/INFLAMMATORY/ALLOGRAFT, 0 ambiguous) → **`pro_tumor_pressure=prior·program_output_change`** (>0 pushes toward malignancy: brake-release on −1 programs or activation of +1; <0 damages a +1 engine / de-represses suppression). **Acquired-weighted parallel `pro_tumor_acquired`** = same but weighting each edge by `max(c_tumor−c_gtex,0)` (collapse-repaired acquired pressure) instead of total abundance → isolates tumor-acquired pro-tumor pressure (miR-375/miR-17-3p top both axes = genuinely acquired; let-7d/f/miR-125a/miR-130a are abundance-only/constitutive). Pilot sets: EMT + PI3K/AKT/MTOR. Results: PI3K master regs MAPK1/AKT1/GSK3B; inhibitor nodes AKT1S1/CDKN1A/CDKN1B/TRIB3 (PTEN sign+1 — its inhibition acts on a lipid not a gene node, a gene-gene-topology limitation); miR-185-5p strongly net pathway-activating (signed −3.83). **`--all-hallmarks`** runs every one of the 50 MSigDB Hallmark sets (combined long-form outputs + a cross-set miRNA summary instead of per-set files): `architecture_all_set_summary` (per set: n_inhibitor_nodes, master regulators, top net-activating arm, prior-based `total_program_output_change`/`total_pro_tumor_pressure`, **+ prior-FREE gene-role malignancy aggregate `total_mal_pro_tumor`=Σ_m mal_pro_tumor / `total_tsg_credit` / `total_onco_collateral` / `malignancy_direction`** — defined for ALL 50 sets incl. the 27 prior=0 ambiguous ones; P53 #1 +53.7, validates vs prior 5/6 on suppressive sets; high-specificity/low-coverage since only ~232 curated drivers contribute), `architecture_all_mirna_pressure` (every (set,miRNA) row), `architecture_all_gene_weights` (incl. `output_influence`), `architecture_mirna_cross_set` (per arm: # sets net-activating vs repressing, mean signed, **plus prior-weighted `n_sets_pro_tumor`/`n_sets_anti_tumor`/`sum_pro_tumor_pressure`/`top_pro_tumor_set`** over polarity-assigned sets). Richest internal topology: APOPTOSIS/COAGULATION/E2F (12-13 inhibitor nodes); strongest net pathway-activating (set,miRNA): COAGULATION×miR-146b-5p (−10.6), COMPLEMENT×miR-181a-5p (−8.9), ESTROGEN_EARLY×miR-191-5p (−9.4). **Pro-tumor leaders** (prior×topology, calibrated sign): miR-375→ALLOGRAFT_REJECTION (+30.6, immune evasion), miR-200a-5p→P53 (+29.3, pro-tumor in 5/5), miR-222-3p (+14), miR-130a-3p, miR-17-3p→P53 (+9.5); **net anti-tumor** = classic suppressors miR-30a-5p (−137), let-7a-5p (−97), miR-24-3p, miR-29b/c. **Note**: the calibrated sign reclassified miR-21-5p to net-anti-tumor (+0.4; it mostly *damages* EMT/KRAS/PI3K engines it represses; its P53/APOPTOSIS brake-release is real but smaller) — supersedes the old binary-sign +45.9. +1 proliferation engines net ~0 pro-tumor (miRNAs damage rather than amplify them). **Family + total rollups** (Q7): `architecture_family_{set_pressure,cross_set}.tsv` (seed-approx name-stem family key; miR-30/29/let-7 families net-anti-tumor, miR-375/200 pro-tumor) + per-set `total_program_output_change`/`net_program_direction`/`total_pro_tumor_*` (all 27 ambiguous prior=0 sets are net-`damaged`). **Brake-release annotation** (Q6): `architecture_mirna_cross_set.tsv` carries each arm's own Stage-1 trajectory `own_dHT`/`own_log2fc`/`own_pressure_move`/`own_acquired_axis` (anti-tumor TS-miRNAs are rank-flat but magnitude-up → not lost). **Gene-role (malignancy-weighted) score**: per (set,miRNA) `mal_pro_tumor = Σ −malignancy_sign·c_tumor` (from `gene_roles.py`) credits repressing a TSG as pro-tumor **independently of the gene-gene topology** → fixes the miR-21 blind spot (topology `pro_tumor_pressure` +0.4 but `sum_mal_pro_tumor` **+34.5, rank #1**, repressing PTEN/PDCD4/FOXO1/3/TGFBR2/VHL); cross-set adds `sum_mal_pro_tumor`/`sum_mal_pro_tumor_acquired` (weight max(c_tumor−c_gtex,0))/`n_tsg_targets`/`n_onco_targets`. Top malignancy-weighted arms = validated oncomiRs (miR-21/182/10b/93/25/221/9/10a); the **acquired** variant shows miR-21's TSG repression is 94% tumor-acquired (32.3/34.5) while miR-374a is constitutive (drops out). Note `output_influence` (signed Katz propagation) is the all-gene continuous downstream-weight; the gene-role `malignancy_sign` is binary ±1 + driver-only, with magnitude graded for drivers via `w_arch`. **Continuous all-gene malignancy** `mal_pro_tumor_cont`=Σ_g dep_role_weight·c_tumor (DepMap breast gene-effect, §gene_roles; cross-set `sum_mal_pro_tumor_cont`, set `total_mal_pro_tumor_cont`, +acquired) scores 530/543 arms (vs driver-only binary) and resolves the coverage blind spot — miR-24-3p +4.7 binary → **−112.6 continuous #1 anti-tumor** (represses many non-driver dependency genes); top dependency-repressed sets = proliferation engines (G2M −362/E2F −330/MYC −190). One-sided (anti-tumor/dependency tail only; corr↔binary 0.29). **Per-(set,gene) hit rollup** (`set_gene_hits`, `--hits-only` rebuilds from existing outputs, no re-fetch → `architecture_set_gene_hits.tsv`): localizes each set's incoming tumour miRNA pressure to specific genes — `incoming_pressure_tumor`(Σ_m c_tumor) × `output_influence` = `prog_change_gene` (topology/hub view) and `mal_hit=−malignancy_sign·incoming_pressure` (gene-role view: TSG pressure = pro-tumor), with `is_hub` (top-decile centrality) + `top_arm`. Reads both lenses at gene resolution: hub hits are mostly program effectors (let-7→AURKA/PLK1 damages E2F) while malignancy hits are the TSGs; striking convergence = **TP53 itself the dominant P53-program node hit, by miR-375**. Tolerates malformed/partial OmniPath chunks (set → flat architecture). **Requires internet** (first run) to populate the OmniPath cache | `mirna_state_class/mirna_gene_edge_class.tsv` + `mirna_state_class.tsv`, Hallmark sets, OmniPath REST | `tissue_reference/geneset_architecture/architecture_<SET>_{gene_weights,induced_edges,mirna_pressure}.tsv` (pilot) or `architecture_all_{set_summary,mirna_pressure,gene_weights}` + `architecture_mirna_cross_set.tsv` + `architecture_family_{set_pressure,cross_set}.tsv` + `architecture_set_gene_hits.tsv` (all-hallmarks) + `network_cache/` |
| `decoupling_validation` | `decoupling_validation.py` | **Validates `lost`/`nat_decoupled` edges** with confound-safe **conditioning** tests (no raw-expression direction). (1) **platform-matched confidence** `nat_anchored_confident` = partial-ρ negative in NAT (same platform as tumor, ESTIMATE-adjusted) and ≈0 in tumor (37/576). (2) **module control** `decoupling_mechanism` (joins Stage-2 module composite): `arm_wide_failure` (254 — whole module decouples) vs `target_specific_escape` (133 — module stays repressed, single edge breaks) vs intermediate/unknown. (3) **competing-regulator / epigenetic conditioning** — re-fit the tumor edge partial-ρ with one extra covariate `v` and measure `{v}_reemergence = rho_base − rho_plus_v`, flagging `{v}_masked` when repression re-appears (reemergence≥0.10 AND rho_plus_v<−0.10): **(3a)** `target_cn` = ASCAT3 copy number (**0/576 cnv_masked** → not copy-number-driven); **(3b)** `target_meth` = per-sample promoter β over the **378 decoupled-target Hallmark genes** (`hallmark_gene_methylation.tsv.gz`, ~785 tumors — validated against Hallmark genes, NOT the APM panel; built chunked via `_build_meth_chunked.py`; covers **561/576 edges, 0 meth_masked** → not methylation-driven); **(3c)** `target_atac` = per-sample promoter ATAC over the **same 378 decoupled-target genes** (ATAC matrix was *also* APM-panel-limited like meth — rebuilt via `analysis.chromatin.per_sample_validation --genes`, `chromatin_per_sample_hallmark/`), **direct** where assayed (n≈74) else **PAM50 subtype-representative** mean of same-subtype assayed samples, with `atac_frac_imputed` recording how much of the covariate is subtype-imputed (imputed cohort-wide: **562/576 edges, 1 atac_masked** — but the covariate is ~93% subtype-imputed, diluting signal). Also a **strict direct-only proof-of-concept** (`atac_direct_*` columns): base + conditioned partial-ρ recomputed on ONLY the ~70 directly-assayed tumors (no imputation) → **466/576 testable, 12 atac_direct_masked** (repression re-emerges on measured accessibility) — ERBB2-led (miR-193a-5p −0.02→−0.28, miR-331-3p −0.24→−0.55), plus CCND1/FGFR1/IGF1R; the imputed test washed all 12 out (0 overlap). Strict is the preferred ATAC readout; imputed kept as the cohort-wide sensitivity view. (4) **gene-level realized repression / `escape_scope`** (joins `mirna_comovement/gene_corepression.tsv`): each lost edge inherits its target's net-repression state → `edge_local_escape` (106: gene still net-repressed by other regulators), `gene_wide_derepression` (185: gene lost net miRNA control), `gene_not_repressed` (285: gene never realizedly repressed). The symmetric **gained** view is in `mirna_comovement/edge_gene_repression_scope.tsv` (`gene_repression_scope` over ALL edge classes): for `acquired_realized` edges, `gene_wide_acquired_repression` (13: gene newly under net repression — miR-21-3p→SMAD7, miR-744-3p→TGFB1), `reinforces_constitutive` (21), `edge_specific_acquired` (53). NAT caveats: field cancerization + small biased n + admixture → NAT-confident set is a lower bound | `mirna_state_class/mirna_gene_edge_class.tsv` (+ `gene_share_*`), `mirna_comovement/{target_corepression,gene_corepression}.tsv`, tumor miRNA/RNA + ESTIMATE + ASCAT3 CNV + `hallmark_gene_methylation.tsv.gz` + per-sample ATAC | `tissue_reference/decoupling_validation/decoupling_validation.tsv` (+ `adj_rho_tumor_plus_{meth,atac}`, `{meth,atac}_reemergence`, `{meth,atac}_masked`, `adj_rho_tumor_direct{,_plus_atac}`, `atac_direct_reemergence`, `atac_direct_masked`, `n_atac_direct`, `gene_share_{tumor,gtex}`, `gene_share_rank_tumor`, `gene_rho_pressure_{tumor,nat}`, `gene_repression_class`, `escape_scope`) |
| `gene_roles` | `gene_roles.py` | **Curated per-gene cancer role** (oncogene/TSG). `malignancy_sign` = +1 oncogene / −1 TSG / 0 dual-unknown (232 high-confidence drivers: 114 onco, 106 TSG, 12 dual; COSMIC-CGC/OncoKB consensus, BC-prioritised). Repression-direction credit = `−malignancy_sign` (silence a TSG = pro-tumor). Conservative (unlisted genes = 0); extendable via drop-in TSV at `config.GENE_ROLES_OVERRIDE`. **Continuous all-gene counterpart** `load_gene_dependency` (`dep_role_weight` = DepMap 26Q1 CRISPR Chronos breast-panel gene-effect, oriented like −malignancy_sign: <0 = dependency/oncogene-like, ≳0 = TSG-like; ~18.5k genes; builder `_build_depmap_dependency.py` → `data/CCLE/depmap_gene_effect_summary.tsv`, download via `scripts/mirna/download_ccle_depmap_inputs.sh`). One-sided (resolves dependency/anti-tumor tail, weak on TSG/pro-tumor tail). **Within-set hierarchy** `load_tf_census` (curated human-TF census, Lambert 2018, 1,639 TFs; builder `_build_tf_census.py` → `annotations/humantfs_lambert2018_tf.tsv`): identity flag `is_tf` that the topology `w_arch` only weakly captures (~48% of curated TFs below median w_arch) → architecture adds `is_tf`/`hub_tf` (TF∧hub = high-confidence master regulator) + tunable `w_hier`=w_arch·(1+TF_HIER_BOOST·is_tf), **wired into scored columns** `mal_pro_tumor_hier`=Σ−malignancy_sign·c·w_hier (+acquired), `program_output_change_hier` (TF-up-weighted topology damage), `tf_pressure`/`n_tf_targets` (raw pressure on curated TFs) at per-(set,miRNA)/cross-set(`sum_*`)/per-set(`total_*`) levels — ≈arch where no TF hit (333/543 arms, corr 0.99), diverges for 95 TF-hitting arms; set-level re-ranks engines TNFA_NFKB −513→−589 (tf_pressure 137 highest), G2M −646→−701. NOTE the gene's *functional role within the set* (master/effector/inhibitor) is the **topology** (w_arch/output_influence/effect_sign), already per-set and continuous; the oncogenic role and TF identity are orthogonal overlays. Consumed by `geneset_architecture` (`sum_mal_pro_tumor` binary + `sum_mal_pro_tumor_cont` continuous + `is_tf`/`hub_tf`/`w_hier`) and ambiguous-set decomposition | built-in dict (+ optional override TSV) + `data/CCLE/depmap_gene_effect_summary.tsv` + `annotations/humantfs_lambert2018_tf.tsv` | returned in-memory (`load_gene_roles` / `load_gene_dependency` / `load_tf_census`) |
| `cross_state_expression_panels` | `cross_state_expression_panels.py` | **grid per-arm boxplots** (+ sample dots) of log2(PM+1) across GTEx / NAT / tumor / **PAM50** (LumA/LumB/Her2/Basal; Normal excluded). All samples in vector plotted (NaN→0). Presets: `all`, `all_hsa`, `landscape_extended`, `coupling_focus`, `family`. `--tumor-scope matched` restricts tumor + PAM50 to NAT-paired primaries (~103). Paginated `--per-page` | `_state_matrices` + tumor × `PAM50_final` | `tissue_reference/cross_state_expression_panels/{<out>_summary,<out>_manifest}.tsv/json`, `figures/<out>_page*.png` |
| `nat_tumor_umap` | `nat_tumor_umap.py` | **UMAP** miRNA-seq: NAT + tumor (`matched` / `unmatched`) + optional **346 GTEx healthy** (`--include-gtex`, default on). Axis labels = post-hoc linear R² %; subtitle PCA PC1/PC2 ref | TCGA full-barcode + GTEx v10 breast miRNA | `figures/nat_{matched,unmatched}_tumor[_gtex]_umap.png` + coordinates/manifest |
| `edge_prior_refinement` | `edge_prior_refinement.py` | **multi-axis edge-prior overlay (transparent, non-circular)** composing the spine literature weight with **A. TargetScan sequence site strength** (`_load_targetscan_weights`, Σ\|weighted context++\|, folds conservation), **breast literature** (reuses `edge_breast_context`), **B. negative-evidence penalty** (Non-Functional MTI contradictions, reliability `n_func/(n_func+λ·n_nonfunc)`), and **C. guide/passenger** (miRGeneDB `mat=1`/`star=1` cached, mapped to miRBase by a 3-tier scheme: **exact mature-seq → seed(2-8)+arm-end → hairpin propagation**, **65% of HE-edge arms / 89% of edges**; only confirmed passenger arms down-weighted by γ, rest neutral). `refined = log1p(evidence) + w_ts·log1p(ts) + w_brst·log1p(n_breast) + log(reliability) + log(arm_factor)`. Each axis a separate column; per-target re-ranking attributes every flip to its driving axis; per-edge `n_supporting_axes` (max **4**: TS, breast, no-neg, **guide**). **Axis D — APA diagnostic, two sources**: (1) **breast-specific** APAatlas GTEx-breast PDUI (218 breast samples → `breast_pdui_median`, `breast_apa_shortened`; 16% of edges breast-shortened — discriminating; GTEx **normal** breast); (2) pan-tissue PolyASite terminal-exon cluster count (`n_polyA_TE`, ~95% near-universal). **Both gene-level → cancel in within-gene re-ranking, so diagnostic flags NOT weight terms**; site-resolved breast-tumour version deferred. **Also emits**: `edges_mirgenedb_validated_universe.tsv` (high-confidence-miRNA universe variant), `mirgenedb_exoneration_{summary,candidates}.tsv` (discarded arms couple 10.6% vs 23.6% neg-sig → supports the filter; 40 candidates that couple anyway — characterisation, not folded into prior). `--w-ts/--w-breast/--lambda-neg/--gamma-passenger` tunable; `--fetch` refreshes caches | HE edges, TargetScan context scores, breast-context PMID cache, raw miRTarBase (Func + Non-Func MTI), **miRGeneDB guide/star FASTA**, **PolyASite 2.0 atlas**, **APAatlas PDUI matrix** + GTEx v10 sample attrs, GENCODE v49 slim, partial-corr table (exoneration), `mature.fa` (caches under `data/external_cache/{mirgenedb,polyasite,apaatlas}/`) | `tissue_reference/edge_prior_refinement/{edge_prior_refined,per_target_arm_reassignments_multiaxis,edges_mirgenedb_validated_universe,mirgenedb_exoneration_summary,mirgenedb_exoneration_candidates}.tsv`, `figures/edge_prior_refinement.png`, `manifest.json` |
| `edge_breast_context` | `edge_breast_context.py` | **breast-context literature prior + per-target arm re-ranking** (non-circular; no TCGA expression). Collects Functional-MTI **PMIDs** per HE edge → fetches PubMed (MeSH + title + abstract, cached `data/external_cache/pubmed/`) → scores each PMID breast (MeSH breast-neoplasm terms OR breast cell-line / mammary regex) → per-edge `breast_context_score` (fraction, diagnostic) + `n_breast_pmids` (absolute, the re-ranker driver). Within each gene ranks regulators by `pan_weight=log1p(evidence)` vs `breast_weight=log1p(evidence)+β·log1p(n_breast_pmids)`; reports which arm is **top-1 credited** under each, i.e. breast-context reassignments. `--beta-sweep 0.5,1.0,2.0` → robustness table (flip@minβ + stable arm); `--no-fetch` cache-only | raw miRTarBase (`mirtar.csv` Functional MTI rows), HE edges, **PubMed E-utilities** | `tissue_reference/edge_breast_context/{edge_breast_context_scores,per_target_arm_reranking,per_target_arm_reassignments,per_target_arm_reassignment_beta_sweep}.tsv`, `figures/breast_context_reranking_beta*.png`, `figures/breast_context_beta_sweep.png`, `manifest.json` |
| `stratum_characterization` | `stratum_characterization.py` | per-Hallmark target CNV burden + regulatory-miRNA expression by stratum | edges, target-gene CNV, miRNA expr, clinical | `stratum_characterization/hallmark_target_cnv_by_stratum.tsv`, `hallmark_mirna_expr_by_stratum.tsv` |
| `hallmark_interaction` | `hallmark_interaction.py` | AGO-gated Hallmark pressure, anti-correlation (cohort + PAM50), enrichment | edges, miRNA expr, RNA, gate, clinical | `hallmark_interaction/hallmark_pressure_per_sample.tsv.gz`, `hallmark_anticorrelation.tsv`, `hallmark_anticorrelation_by_pam50.tsv`, `hallmark_highev_target_enrichment.tsv` |
| `subtype_contrasts` | `subtype_contrasts.py` | luminal vs non-luminal (Normal aside) anti-corr + Δrho; differential gene pressure; per-Hallmark miRNA impact; immune axis + IRF1 route | edges, gated pressure, RNA, miRNA expr, clinical | `subtype_contrasts/*` |
| `pam50_gene_resolution` | `pam50_gene_resolution.py` | **actual PAM50 subtype per-gene resolution**: LumA/LumB/Her2/Basal one-vs-rest pressure, target RNA, and top driver miRNAs | gated pressure, RNA, miRNA expr, edges, clinical | `pam50_gene_resolution/gene_pressure_by_pam50.tsv`, `gene_pressure_one_vs_rest.tsv`, `gene_mirna_drivers_by_pam50.tsv` |
| `visibility_archetype_contrasts` | `visibility_archetype_contrasts.py` | **actual cold_Basal / hot_Luminal** (APM visibility archetypes): all 50 Hallmarks + tumor-suppressor gene route panel | archetype assignments, gated pressure, RNA, miRNA expr, edges | `visibility_archetype_contrasts/hallmark_embodiment_by_archetype.tsv`, `hallmark_contrast_coldBasal_vs_hotLuminal.tsv`, `gene_route_focus.tsv`, `gene_route_top_mirnas.tsv`, `differential_gene_pressure_archetype.tsv` |
| `gene_expression_explainability` | `gene_expression_explainability.py` | **sample × gene OLS:** nested R² within **cohort** or **each PAM50** (+ RNA-low quartile); CN + CPE + proliferation + meth + SV core, then route pressure / family / arms; IRF1 curated vs TargetScan alt | RNA, CN, meth, SV burden, hub-route edges, miRNA arms, clinical | `gene_expression_explainability/{cohort,pam50_*}/incremental_r2_hub_genes.tsv`, `mirna_term_coefficients.tsv`, `per_sample_residuals.tsv.gz`, `incremental_r2_by_subtype_summary.tsv` |
| `robustness_checks` | `robustness_checks.py` | **grant Aims 1-2 robustness + 1b per-family decomposition:** (1) does basal miRNA→target repression survive proliferation adjustment under **three proxies** (E2F+G2M, MKI67-alone, **E2F/MYC-stripped** metagene) added to CPE+HRD, cohort+Basal — an over-adjustment control + suppressor sign-structure table; (2) is the hub a miRTarBase study-count artifact — recompute basal load under evidence/binary/**TargetScan weighted-context++** weighting; (1b) **per-family load share + within-subtype coupling** — is each top-load family (miR-17~92/106b~25, miR-23~27~24, miR-221/222) basal-*specific* in coupling or merely basal-*enriched* in expression | edges, miRNA expr, RNA, clinical, cached gated pressure, AGO gate, `data/miRNA/Predicted_Targets_Context_Scores.default_predictions.txt` | `robustness/aim1_proliferation/{hub_route_partial_corr,hallmark_coupling_prolif,prolif_sign_structure,hallmark_coupling_by_pam50_prolif,family_coupling_by_pam50,family_specificity_summary}.tsv`, `robustness/aim2_circularity/{mirna_basal_load_by_weighting,hub_rank_summary,per_target_top_regulators,family_load_share}.tsv` |
| `pressure_build` | `pressure_build.py` | **shared spine pressure** (softmax_z_logrpm + evidence_mass on M0 edges); hybrid uses PRESSURE_HYBRID_* |
| `pressure_sensitivity` | `pressure_sensitivity.py` | grid comparing z / softmax / softmax_z × degree / mean (M0 edges, prolif-adjusted coupling) |
| `hybrid_pressure` | `hybrid_pressure.py` | **miRTarBase + TargetScan hybrid modes** (M7/M8/M11 + M0 baseline; M7+ uses softmax_z + combined_mass): M7 primary fusion, M8 sum-tracks, M11 pure TS capped | edges, TargetScan, miRNA expr, RNA, clinical, AGO gate | `hybrid_pressure/{mode}/`, `mode_comparison_summary.tsv` |
| `make_grant_figure` | `make_grant_figure.py` | **grant figure (affirmative):** subtype × Hallmark heatmap of the proliferation-adjusted pressure→repression coupling (Basal-restricted, multi-program, hub-bracketed) + compact Basal proliferation-independence strip; one-column, B&W-safe | `robustness/aim1_proliferation/{hallmark_coupling_by_pam50_prolif,hallmark_coupling_prolif}.tsv` | `figures/grant_figure_basal_heatmap.{pdf,png}` |
| `cptac_validation` | `cptac_validation.py` | **CPTAC proteome validation of the pressure spine**, two cohorts via `--cohort`: **`tcga105`** (same TCGA patients, Mertins iTRAQ n=105; pressure from TCGA miRNA) and **`prospective`** (independent CPTAC-2 prospective breast, n=101; pressure rebuilt self-contained from CPTAC-2's own GDC miRNA-seq — `scripts/cptac/{fetch,ingest}_cptac2_breast_mirna`). Four pressure variants (all-edge/high-evidence × ungated/AGO-gated) × three layers: **L1** RNA→protein gap `rna_z−protein_z` ~ pressure (expect +); **L1b** `protein_resid` = protein residualized on its own mRNA per gene (slope-free "beyond mRNA", expect −); **L2** protein level ~ pressure (expect −) — each per Hallmark + PAM50/TNBC/immune/stage strata. Partial (tcga105: CPE+HRD; prospective: purity+CIN); raw reported | tcga105: `cptac_tcga_*` + map; prospective: `cptac_proteome_cct`/`cptac_rna_cct` + `cptac2_brca_mirna_isoform_log2rpm.tsv` + CLI .tsi | `cptac_validation/{...}` (+ `cptac_validation/prospective/`) |
| `cptac_acquired_pressure` | `cptac_acquired_pressure.py` | **Acquired-pressure (tumor − NAT, paired) version of the gene-centric CPTAC validation** (MH-77). Per-participant `acq(g,p) = pressure_tumor − pressure_NAT` for the 101 TCGA participants with matched tumor+NAT miRNA (cross-state-comparable `softmax_logrpm`, evidence_mass, **identical edge set across states**, ungated; all-edge + high-evidence variants), run through the `cptac_validation` association machinery against **TCGA tumor mRNA** (`mrna_anticorr`, powered n≤101) + the CPTAC tcga105 protein layers (`protein_anticorr`/`protein_resid`/`rna_protein_gap`, **n=14 → empty by the n≥15/≥20 floors**). Partial CPE+HRD. Note: a *population* normal baseline can't substitute (per-gene constant shifts are correlation-invariant) — only paired NAT changes the answer | matched TCGA tumor+NAT miRNA (`edge_acquired_pressure_panels._matched_pair_matrices`), TCGA RNA, CPTAC tcga105 layers, CPE+HRD | `cptac_validation/acquired/{acquired_gene_level_associations.tsv.gz, acquired_pooled_associations.tsv, acquired_hallmark_associations.tsv, method_manifest.json}` |
| `cptac_resid_survivors` | `cptac_resid_survivors.py` | **Single-edge resolution of the CPTAC-2 `protein_resid` survivors** (follow-up to MH-34). For each survivor gene × high-evidence candidate arm: partial Spearman of the arm's own expression vs the gene's protein-beyond-mRNA residual (adj purity+CIN) → which miRNA drives the translational repression; per-PAM50 driver coupling (subtype); literature annotation via miRTarBase functional-MTI + MH-31 breast-context PMIDs. Recovers **PTEN←miR-17~92** at protein. Prospective cohort only | prospective `gene_level_associations.tsv.gz`, CPTAC-2 miRNA arms, `protein_resid`, HE edges, `edge_breast_context_scores.tsv`, CLI strata | `cptac_validation/prospective/resid_survivors/{survivor_drivers,survivor_arm_candidates,survivor_subtype}.tsv` |
| `cptac_orphan_discovery` | `cptac_orphan_discovery.py` | **Orphan-edge discovery via CPTAC protein.** Orphan = (TargetScan `ts_weight≥0.25` OR ENCORI `clipExpNum≥2`) AND not miRTarBase functional-MTI. Screens ~15.5k testable orphan edges over the Hallmark universe: cov-residualized partial Spearman of orphan-arm expression vs gene `protein_z` + `protein_resid`, prospective (primary) + TCGA-105 (replication direction); BH-FDR. Outputs a ranked candidate-edge queue with strength tags + prior support. **Nominations for CLIP/luciferase, not validated edges** (protein-layer analogue of MH-26 `mirtar_gap_coupling_queue`). Method self-validated by de-novo recovery of miR-29→collagen | Hallmark universe, TargetScan weights, ENCORI CLIP pairs, HE edges, CPTAC-2 + TCGA-105 miRNA arms + `protein_z`/`protein_resid`, clinical | `cptac_validation/orphan_discovery/{orphan_candidates.tsv,orphan_edges_tested.tsv.gz,method_manifest.json}` |
| `cptac_target_specificity` | `cptac_target_specificity.py` | **Target-specificity (decoy/placebo) control on the MH-35 survivor drivers.** For each `survivor_drivers` arm: cognate `protein_resid` partial Spearman (purity+CIN) vs the *same* arm's coupling to every Hallmark-universe protein it does **not** target (decoys = universe − [miRTarBase any ∪ TargetScan seed ∪ ENCORI CLIP]; ≥50 decoys). Reports cognate ρ, decoy distribution (median/mean/sd/frac-neg), empirical left-tail p (frac decoys ≤ cognate), specificity z, decoy-vs-0 Wilcoxon, and a `specific` flag (emp p<0.05 & cognate<0). Tests whether the survivor couplings are **target-selective vs broadly negative** (global confound). **Batch pass** (default `--batch-kind plex`; `none`/`site`/`auto` selectable): re-runs each pass with **batch dummies** (`cptac_batch`: literal TMT plex for prospective, else enrolling-site proxy) added to purity+CIN and flags `batch_robust` edges (`*_batch` columns). Prospective cohort | `survivor_drivers.tsv`, CPTAC-2 miRNA arms + `protein_resid`, HE + orphan (TargetScan/ENCORI) edges for exclusion sets, CLI strata (purity/cin) + `cptac_batch` plex/site | `cptac_validation/prospective/target_specificity/{arm_specificity.tsv,decoy_couplings.tsv.gz,method_manifest.json}` |
| `tcga_batch` | `tcga_batch.py` (+ `scripts/tcga/fetch_tcga_brca_seq_batch.py`) | **TCGA-BRCA sequencing batch covariates (ready for full refresh; NOT yet wired into the spine).** `augment_cov(base_cov, kind)` adds the literal TCGA analyte **plate** (MBatch BatchId, aliquot-barcode field 6) for miRNA-seq and/or RNA-seq, or the coarser **TSS** proxy. `kind ∈ {none,tss,plate(RNA),mirna_plate,plate_both}`; plates with <30 samples pooled (→ ~14 levels/assay, <20). Map fetched from GDC for both assays' primary-tumor aliquots (1097/1101 participants; 41 miRNA / 43 RNA plates). Intended opt-in for `target_combined_anticorr`/`hallmark_interaction`/`robustness_checks` at the next full refresh | GDC files API (`api.gdc.cancer.gov`) | `data/TCGA/tcga_brca_seq_batch.tsv` (+ `.manifest.json`) |
| `cptac_batch` | `cptac_batch.py` (+ `scripts/cptac/fetch_cptac_brca_tmt_plex.py`) | **Shared CPTAC batch covariates.** `augment_cov(base_cov, cohort, kind)` adds MS-plex or enrolling-site dummies (reference dropped, rare levels pooled). **Literal MS plex:** prospective = TMT plex (PDC study **PDC000120**, 15 plexes → `data/CPTAC/cptac_brca_prospective_tmt_plex.tsv`); tcga105 = iTRAQ "Experiment Order" from the Sample-Mapping xlsx. **Site:** TSS code from the sample id (always-available accrual proxy shared by miRNA-seq + protein). Consumed by MH-34/35/36/39/40 via `--batch-kind {none,site,plex,auto}` (writes a `batch_<kind>/` subdir) | PDC GraphQL (`studyExperimentalDesign` + `biospecimenPerStudy`); iTRAQ Sample-Mapping xlsx | `data/CPTAC/cptac_brca_prospective_tmt_plex.tsv` (+ `.manifest.json`) |
| `buffa_validation` | `buffa_validation.py` | **Independent-cohort replication** of edge coupling in the **Buffa 2011** cohort (GEO **GSE22216** miRNA + **GSE22219** mRNA, 207 paired primary breast tumours, **no TCGA overlap**). Per-edge raw + partial Spearman (miRNA arm vs target mRNA, adj **E2F/G2M prolif + ER**; Buffa lacks purity/HRD) for **HE edges** (compared edge-by-edge to TCGA `edge_partial_corr` partial-ρ: concordance, sign-replication) and **TargetScan-orphan edges** (does the MH-23 HE>orphan coupling gap reproduce?). Legacy bare arm names mapped by bare stem (guide-accurate). **Sample pairing by clinical fingerprint** (age/size/nodes/ER/grade/DRFS — the GEO title index is NOT the pairing). Builds paired matrices cached under `data/external_cache/geo/` | GSE22216/22219 series matrices + GPL8178/GPL6098 annot, HE edges, TargetScan orphan builder, TCGA partial-corr table | `output/buffa_validation/{he_edge_coupling_buffa,he_edge_coupling_buffa_vs_tcga,orphan_edge_coupling_buffa}.tsv`, `figures/buffa_vs_tcga_replication.png`, `method_manifest.json` |
| `dcis_timing` | `dcis_timing.py` (+ loader `dcis_geo_loader.py`) | **In-situ→invasive TIMING of the healthy→tumour miRNA-abundance signature (MH-48).** GSE59247 (Agilent GPL15019; 26 DCIS + 14 IBC) loaded as a **labelled state set** (authoritative `tissue type`, 4 technical-replicate pairs collapsed, Ambion controls dropped; **no reliable matched synchronous DCIS+IBC pairs**) — explicitly **NOT** a linear `healthy→NAT→DCIS→tumour` index. Per arm: acquired direction = **same-modality** GTEx-anchored abundance trajectory (`gtex_to_tumor_pct`; GTEx-absent + `tumor_induced`→+1), magnitude = **same-platform** DCIS-vs-IBC Mann–Whitney + rank-biserial, TCGA used for **direction only** (rank). v16→modern arm **bridge** (exact arm-suffix → stem grouping with abundance resolution: non-star=guide, `*`=passenger; `bridge_method` per arm). Classifies acquired-axis arms EARLY_or_underpowered / LATE_invasion_coupled / DIVERGENT (BH per acquired-axis family) + nominal p<0.05 tier + **set-level Wilcoxon signed-rank** (the powered statement at n_ibc=14) + **intra-DCIS grade gradient** (Spearman vs grade 1/2/3, no healthy anchor needed). Outcome (3 recurrence events) reported, not modelled | GSE59247 series matrix (`data/external/dcis_geo/`), `cross_state_landscape/mirna_arm_de_landscape.tsv` (acquired abundance direction), `mirna_state_class/mirna_state_class.tsv` (pressure-axis annotation) | `output/dcis_timing/{dcis_insitu_invasive_timing,dcis_timing_by_pam50,dcis_grade_gradient}.tsv, method_manifest.json` |
| `dcis_mrna_coupling` | `dcis_mrna_coupling.py` (+ loader `dcis_geo_loader.load_gse59246`) | **Within-patient miRNA↔mRNA coupling corroboration of MH-48 (MH-49).** GSE59247 miRNA + GSE59246 mRNA are companion measurements on the same lesions → paired by `rep_group` (**40 paired DCIS+IBC samples**). Per high-evidence edge: within-sample Spearman(arm, target) across paired samples; BH q; **sign-concordance + binomial replication vs TCGA** `coupling_permutation/coupling_edge`; loss-leader-vs-background MWU; loss-leader target de-repression DCIS→IBC. GPL13607 numeric feature→gene via persisted `data/external/dcis_geo/GPL13607_feature_to_gene.tsv.gz`. **Finding:** TCGA-neg edges replicate 59% within-sample (p=2.2e-14); loss-leaders more negative (p=0.01); miR-29c→collagen/ECM (SERPINH1/COL4A1/LOX) confirmed within-patient | GSE59247+GSE59246 series matrices, GPL13607 map, `output/edges/mirna_hallmark_edges.tsv.gz`, `coupling_permutation/coupling_edge.tsv.gz` | `output/dcis_mrna_coupling/{within_sample_edge_coupling,loss_leader_target_derepression}.tsv, method_manifest.json` |
| `dcis_ev_progression` | `dcis_ev_progression.py` | **EV miRNA export across the isogenic MCF10A progression (MH-50).** GSE297448 exosomal miRNA-seq, MCF10A→AT1→DCIS→CA1a (Normal→benign→DCIS→invasive, 3 reps/stage) — ordered trajectory legitimate because **isogenic**. Catches the global EV-cargo confound (95% of miRNA rise) → **CLR compositional EV-share**; per-miRNA stage-ordinal Spearman + DCIS→invasive step; **export-selectivity** = cellular LOSERS gain EV-share vs GAINERS (MWU), cross-referenced to the independent TCGA cellular direction. **Finding:** selective export (p=0.027) but arm-specific — miR-126-3p/miR-140-3p exported, miR-29c retained (cellular loss). NB exact cell-line-token stage map (`MCF10AT1`≠`MCF10A` prefix) | `data/external/dcis_geo/GSE297448_Normalized_microRNA_readcounts_All_samples.txt.gz`, `cross_state_landscape/mirna_arm_de_landscape.tsv` (cellular direction) | `output/plasma_ev/mcf10a_progression/{ev_progression_trend.tsv, method_manifest.json}` |
| `dcis_mir29c_gse93740` | `dcis_mir29c_gse93740.py` | **Cellular MCF10A isogenic progression + DIRECT EV export (MH-51).** GSE93740 cellular miRNA (P→AT1→NeoT→DCIS→CA1, 3/stage); per-arm cellular Spearman+Jonckheere; miR-29c/loss-leader validation. **Direct export** = cross cellular trend (this) × EV-share trend (GSE297448, same MCF10A lineage) → cell-down+EV-up; no TCGA borrow. Finding: miR-29c-3p cell-intrinsic decline ρ=−0.88 (arm-specific; miR-29a/b rise); export minority (43 arms), miR-140-3p exported, miR-29c depleted | `GSE93740_bedrosian-mirna.exact4.txt.gz`, GSE297448 EV (via `dcis_ev_progression`) | `output/plasma_ev/mcf10a_cellular/{cellular_vs_ev_direct_export,cellular_progression_trend}.tsv, method_manifest.json` |
| `dcis_lcm_compartment_gse162670` | `dcis_lcm_compartment_gse162670.py` | **LCM composition-reduced progression replication / tiebreaker (MH-51).** GSE162670 LCM-microdissected TNBC miRNA by morphology → normal(14)/DCIS(12)/invasive(48); per-arm Spearman+Jonckheere vs morphology ordinal. Tiebreaker for the composition question: in LCM-purified epithelium does miR-29c still decline? Finding: **n.s.** (ρ=−0.14, q=0.70) → consistent with composition-driven in bulk. NB morphology- not compartment-resolved (plan contingency); messy GEO-template matrix parsed by skipping `#` comments to the `ID_REF` row | `GSE162670_RMA_normalized_matrix.txt.gz`, `cross_state_landscape/mirna_arm_de_landscape.tsv` | `output/dcis_lcm_gse162670/{lcm_progression_trend.tsv, method_manifest.json}` |
| `dcis_htan_compartment` | `dcis_htan_compartment.py` | **Compartment test of the invasion miRNA programs in Risom 2022 DCIS MIBI (effector level; MH-52).** Resolves the MH-51 stromal question on 69,672 cells (normal/DCIS/IBC): (ii) fibroblast/CAF + myoepithelial fraction + CAF-activation ratio vs (i) within-fibroblast ECM trend (COLI/SMA/FAP/VIM/MMP9) + CAF-vs-NORMFIBRO; Kruskal + Jonckheere + MWU. **Result:** myoep loss 0.24→0→ (JT p<1e-3); CAF-activation ratio 0.03→0.44 (p=3e-4); COLI/FAP/SMA rise + CAF≫NORMFIBRO ⇒ regulatory CAF activation, not fraction shift. Loader uses real Risom schema (Tissue_Type/celllineage/phenotype) with present-checked fallbacks. **miRNA caveat:** MIBI protein → miR-29 targets + compartments, NOT miR-29c. Data: Mendeley `d87vg86zd8` single-cell CSV in `data/external/dcis_mibi/` (file API OAuth-gated → placed manually) | Risom single-cell table + `tissue_features/Table_S4_ECM_genes_CAF_high_low.csv` (cross-ref) | `output/dcis_htan_compartment/{compartment_fraction_by_stage,fibroblast_ecm_by_stage}.tsv, method_manifest.json` |
| `dcis_caf_nf_gse37527` | `dcis_caf_nf_gse37527.py` | **Non-circular CAF-vs-NF miRNA test (MH-54).** GSE37527 = 6 paired breast CAF vs matched NF primary cultures, miRNA array (GPL14767 Feature-Number → resolves miR-29a/b/c). Paired Wilcoxon CAF vs NF + BH; focus miR-29 family + loss-leaders. **Result:** miR-29c NOT down in CAFs (p=0.69) → CAF ECM-up is miR-29-independent; miR-143/145/126/205/185 are CAF-down (p=0.031). The directly-measured (non-circular) miRNA test MIBI/HTAN protein can't give. GPL14767 map persisted | GSE37527 series matrix + `GPL14767_feature_to_mirna.tsv.gz` (`data/external/caf_nf_gse37527/`) | `output/dcis_caf_nf_gse37527/{caf_vs_nf_paired.tsv, method_manifest.json}` |
| `dcis_caf_tgfb_gse196354` | `dcis_caf_tgfb_gse196354.py` | **Direct TGFβ mRNA test in matched breast CAF/NF (MH-54 addendum).** GSE196354 = 7 paired NF/CAF RNA-seq (FPKM, gene symbols). Paired Wilcoxon CAF vs NF for TGFβ-pathway / myCAF / ECM-collagen sets + **positive control (FAP/ACTA2) + null control (housekeeping)**; per-sample median-centred (global-shift fix). **Result:** positive control weak + collagens flat → cultured CAFs are culture-drifted (not myCAF-activated); TGFβ-desmoplasia conclusion rests on in-vivo Risom, not this. Exemplifies the apm-rigor-protocol control checks | GSE196354 FPKM (`data/external/caf_nf_mrna_gse196354/`) | `output/dcis_caf_tgfb_gse196354/{caf_vs_nf_tgfb_ecm.tsv, method_manifest.json}` |
| `dcis_orphan_rigor` | `dcis_orphan_rigor.py` | **Composition-rigor retrofit of the TS/ENCORI orphan list (MH-55).** Re-tests the 492 orphan edges (MH-36/38) in within-patient DCIS (GSE59247+59246, 40 paired) raw vs composition+prolif+batch-adjusted partial Spearman → which orphan nominations survive composition adjustment. Finding: ECM orphans preferentially attenuated (neg 84%→62%, median ρ halved); surviving core let-7d→COL1A2 / miR-29a→COL1A1. Reuses `_pair`/`composition_covariates`/`partial_spearman` | `buffa_validation/orphan_triple_cohort_validation.tsv`, GSE59247+59246 | `output/dcis_orphan_rigor/{orphan_composition_retrofit.tsv, method_manifest.json}` |
| `dcis_risom_outcome_niche` | `dcis_risom_outcome_niche.py` | **Further Risom leverage (MH-56): DCIS outcome + spatial niche.** Outcome = progressor(case) vs non-progressor(ctrl) DCIS per-FOV compartment metrics; spatial = is collagen-high fibroblast in myoepithelium-poor neighbourhood (Neighbor_dist_MYOEP). Finding: myoep-loss spatially coupled to desmoplasia (p=0.002/0.007); outcome directionally matches Risom but underpowered | `data/external/risom2022_dcis_mibi/risom2022_single_cell.csv` | `output/dcis_risom_outcome_niche/{dcis_outcome_per_fov.tsv, method_manifest.json}` |
| `outcome_advanced` | `outcome_advanced.py` | **Phase 3 (MH-65): regulatory-STATE survival, both cohorts.** #1 per-patient coupling coherence/decoupling, #2 NMF pressure-state factors, #3 RSF nested-CV (pressure vs clinical). Finding: coherence→TCGA-OS q=0.004 (unreplicated); no cross-cohort-robust signal | edges, pressure, Buffa, sksurv | `output/outcome_advanced/*` |
| `outcome_program_role_topology` | `outcome_program_role_topology.py` | **Phase 3 (MH-64 add): program×gene-role pressure + topology prior** vs TCGA DFI+OS. Each program's genes split by role (all/TF/onco/TSG), aggregated unweighted vs topology-weighted (architecture w_arch). Finding: 0/228 FDR; topology no help | L1 gene pressure, architecture_all_gene_weights | `output/outcome_program_role_topology/*` |
| `outcome_extra_axes` | `outcome_extra_axes.py` | **Phase 3 (MH-64 add): extra outcome axes** — #4 miRNA-CNV dosage (per-locus+burden) + #7 pressure-residual burden vs TCGA DFI+OS. Finding: CNV null; residual→OS borderline | `mirna_locus_cnv` sample_entity_cnv, `gene_expression_explainability` residuals | `output/outcome_extra_axes/*` |
| `outcome_pressure_variants` | `outcome_pressure_variants.py` | **Phase 3 (MH-64): comprehensive pressure-variant × resolution × gene-role survival + multivariate signature.** 12 variants × {program/gene/TF/onco/TSG/driver/HE-target} vs DFI+OS + elastic-net CV. Finding: pressure not robustly prognostic (DFI 0 FDR; multivariate CV pressure ≤ clinical) | layer/model per-sample pressure, gene_roles, TCGA clinical | `output/outcome_pressure_variants/*` |
| `outcome_metabric` | `outcome_metabric.py` | **Phase 3 (MH-63): cBioPortal METABRIC powered per-subtype** (2509 mRNA, RFS+OS, CLAUDIN). Hallmark program scores + target genes. Finding: proliferation→RFS (q<0.001), hypoxia-score null, MKNK2/KCNJ8 null | `data/METABRIC/` mRNA+clinical | `output/outcome_metabric/*` |
| `outcome_buffa_pressure` | `outcome_buffa_pressure.py` | **Phase 3 (MH-62): pressure→DRFS in the independent Buffa/METABRIC-miRNA subset** (matched miRNA+mRNA, Buffa-native AGO-gated pressure). Finding: pressure NULL, miR-210 expression strong (=Buffa2011 known); TCGA pressure-outcome non-replicated | `buffa_validation.load_buffa`, GSE22216 series DRFS | `output/outcome_buffa_pressure/*` |
| `outcome_pressure_survival` | `outcome_pressure_survival.py` | **Phase 3 (MH-61): does PRESSURE predict survival, which resolution?** 9 resolutions (expr arm/miRNA/family, AGO gate, gene/hub/program/aggregate/role pressure) vs TCGA DFI+OS. Finding: pressure>expression (gene-pressure MKNK2 + AGO-capacity FDR; expression 0) | per-sample pressure matrices, TCGA clinical | `output/outcome_pressure_survival/*` |
| `outcome_subtype` | `outcome_subtype.py` | **Phase 3 (MH-61): focused per-subtype confirmation** of cohort hits — LumA/LumB/Basal (TCGA) + ER+/ER- (GSE22216). Finding: gene-pressure→Basal, AGO→LumA, miR-210-3p risk pan-ER | sweep outputs, PAM50, GSE22216 ER | `output/outcome_subtype/*` |
| `spatial_common` | `spatial_common.py` | **Phase 3 SPATIAL toolkit (MH-64).** Shared engine for every spatial layer: `program_gene_sets`/`score_programs` (5 Hallmark programs), `deconvolve_matrix` (Wu-signature NNLS), `compartment_fractions`, `analyse_spots`, `gene_level_pressure_retest` (the spatial MH-56d classifier: brake-release/concordant-repression/discordant), `morans_i`, 10x mtx/h5 loaders | `coupling_edge`, `gene_pressure_by_state`, `gene_roles`, Wu signature | (library; self-test `_spatial_selftest.py`) |
| `spatial_mibi_anchor` | `spatial_mibi_anchor.py` | **Phase 3 SPATIAL — single-cell PROTEIN anchor (MH-64).** Risom DCIS MIBI (69,672 cells). Program markers across normal→DCIS→IBC + the rigorous MH-56d retest on epithelial-lineage progression axis. Validates the engine (must reproduce SLC2A1 brake-release + ERBB2/ESR1 concordant-repression). Finding: 3/3 engine gate PASS | `risom2022_dcis_mibi/risom2022_single_cell.csv` | `output/spatial_mibi_anchor/{mh56d_spatial_retest.tsv, program_localization_by_stage.tsv, program_marker_by_compartment.tsv}` |
| `spatial_visium_wu` | `spatial_visium_wu.py` | **Phase 3 SPATIAL — primary-tumour SPOT (MH-64).** Wu Visium (Zenodo 4739739), 6 primary BRCA. Per-spot program scoring + deconvolution → compartment localization, Moran's I niche structure, anchor brake-release co-localization. Finding: programs niche-structured (Moran 0.45–0.60); SLC2A1→glycolysis, FAP→EMT across 6 sections | `data/external/brca_visium_wu/` | `output/spatial_visium_wu/{program_compartment_localization.tsv, anchor_brake_release_localization.tsv, per_section_morans_i.tsv}` |
| `spatial_visium_public` | `spatial_visium_public.py` | **Phase 3 SPATIAL — SPOT replication (MH-64).** Independent 10x Human Breast Cancer block (3,798 spots). Same pipeline as Wu → cross-dataset robustness. Finding: replicates localization + SLC2A1/FAP brake-release | `data/external/brca_visium_10x/` | `output/spatial_visium_public/*` |
| `spatial_xenium` | `spatial_xenium.py` | **Phase 3 SPATIAL — single-cell RNA (MH-64).** 10x Xenium breast Rep1 (167,780 cells, 313-gene panel). Single-cell compartment typing + program localization (no deconvolution) + niche structure. Finding: proliferation→epi, EMT→CAF, immune→immune at single-cell; panel-limited (released anchors off-panel) | `data/external/brca_xenium/` | `output/spatial_xenium/{program_by_compartment.tsv, method_manifest.json}` |
| `spatial_synthesis` | `spatial_synthesis.py` | **Phase 3 SPATIAL — cross-resolution synthesis + MH-64.** Integrates the 4 layers: dominant compartment per program per layer, Moran's I consensus, MH-56d verdict, brake-release co-localization. Finding: localization unanimous across all resolutions; decoupling gene-specific + spatially real | the 4 spatial-layer manifests | `output/spatial_synthesis/{cross_resolution_localization.tsv, cross_resolution_morans.tsv, decoupling_verdict.json}` |
| `spatial_malignancy_retest` | `spatial_malignancy_retest.py` | **Phase 3 SPATIAL push #1 (MH-66) — full MH-56d retest on primary Visium via an inferCNV malignancy axis.** Per-spot malignancy = genome-wide CNV energy vs marker-diploid reference (no deconvolution); retest among epithelial spots, own-chromosome-removed circularity guard. Finding: SLC2A1 brake-release reproduces; concordant-repression is axis-specific (transition, not intratumoral grade) | `data/external/brca_visium_wu/`, `brca_visium_10x/`, `gencode.v49.slim.parquet` | `output/spatial_malignancy_retest/{anchor_malignancy_retest.tsv, malignancy_retest_pooled.tsv}` |
| `spatial_coexpression_niche` | `spatial_coexpression_niche.py` | **Phase 3 SPATIAL push #2 (MH-66) — neighbourhood co-occurrence.** Bivariate Moran's I (kNN + 199-perm null) for released genes vs their programs across 7 primary Visium sections. Finding: SLC2A1↔glycolysis / FAP↔EMT / VIM↔EMT co-occupy physical neighbourhoods, perm-sig 7/7 (hardens Finding 3 beyond global correlation) | `data/external/brca_visium_wu/`, `brca_visium_10x/` | `output/spatial_coexpression_niche/{neighbourhood_cooccurrence.tsv, per_section_bivariate.tsv}` |
| `outcome_survival` | `outcome_survival.py` | **Roadmap Phase 3 outcome (MH-60): miRNA arm → survival, cross-cohort.** Cox per headline arm: TCGA OS (composition-adjusted) + GSE22216 DRFS (independent). Finding: single-arm weak (0 FDR), 3 cross-cohort-concordant protective arms | TCGA `load_mirna_arms`+unified clinical, GSE22216 arm matrix+series DRFS | `output/outcome_survival/{arm_survival.tsv, method_manifest.json}` |
| `core_coupling_deconv_retest` | `core_coupling_deconv_retest.py` | **Phase 1-refined (MH-71): headline composition-retest with BayesPrism deconvolution fractions** (vs MH-57 metagenes). Finding: 96% couplings stay negative (>93%), miR-29→ECM the exception — framework more robust under gold-standard composition adjustment | BayesPrism fractions, coupling_edge, TCGA | `output/core_coupling_deconv_retest/*` |
| `compartment_annotate_network` | `compartment_annotate_network.py` | **Roadmap Phase 2a (MH-59): compartment-annotate the network.** Assigns each framework target gene to its peak scRNA cell type → 5 coarse compartments; annotates headline edges. Finding: miR-29→ECM targets CAF-enriched (explains MH-57 attenuation); ~19% of targets epithelial-specific | Wu GSE176078 scRNA, `coupling_permutation/coupling_edge.tsv.gz` | `output/compartment_annotate_network/{gene_compartment_map.tsv, edges_compartment_annotated.tsv, method_manifest.json}` |
| `cell_line_subtype_fidelity` | `cell_line_subtype_fidelity.py` | **Cell-line↔tumour-subtype fidelity across modalities (MH-68).** Builds TCGA PAM50 centroids in mRNA + miRNA-pressure gene space; matches curated breast lines to nearest subtype; TCGA tumour-only split as the disambiguating control. Finding: cell lines track subtype at mRNA (coarse 100%); pressure subtype-informative in tumours (held-out 66%) but collapses for cell lines (≈chance) → isolates cell-line NanoString miRNA as weak link | TCGA `load_rna`/`load_clinical_strata`/`compute_gene_pressure`, `data/CCLE/{CCLE_miRNA_20181103.gct, OmicsExpressionTPMLogp1HumanProteinCodingGenes.csv}`, `cell_line_subtype_map` | `output/cell_line_subtype_fidelity/{method_manifest.json, line_subtype_match.tsv}` |
| `ccle_breast_target_anticorr` | `ccle_breast_target_anticorr.py` | **Phase 4c breast-retarget (MH-67): framework edges across the CCLE BREAST panel.** miRNA↔target Spearman across 50 breast lines (NanoString miRNA + DepMap RNA-seq), overall + per intrinsic subtype + subtype-adjusted, with decoy specificity + gold-standard positive control. Finding: edges positively correlated AND positive control (miR-200→ZEB1) also fails ⇒ cross-line axis insensitive to repression; INCONCLUSIVE, vindicates within-cohort design | `coupling_permutation/coupling_edge.tsv.gz`, `data/CCLE/{CCLE_miRNA_20181103.gct, OmicsExpressionTPMLogp1HumanProteinCodingGenes.csv, Model.csv}` | `output/ccle_breast_target_anticorr/{method_manifest.json, edge_ccle_breast_anticorr.tsv, positive_control_pairs.tsv, family_ccle_summary.tsv}` |
| `sc_mirna_target_k562` | `sc_mirna_target_k562.py` | **Roadmap Phase 4c (MH-65): the only MEASURED per-cell miRNA↔target test.** GSE114071 half-cell co-seq (19 K562 cells, paired miRNA-half + mRNA-half by HalfCell id); tests 1,013 headline edges as per-cell Spearman with same-arm decoy specificity + max-power positive control (abundant miRNA→broad curated targets). Finding: headline null, positive control only weakly directional ⇒ data-limited INCONCLUSIVE (not a refutation); miR-92a-3p carries expected sign. Non-circular | GSE114071 (`data/external/sc_mirna_mrna_gse114071/`), `coupling_permutation/coupling_edge.tsv.gz`, `edges/mirna_hallmark_edges.tsv.gz` | `output/sc_mirna_target_k562/{method_manifest.json, edge_sc_anticorr.tsv, abundant_arm_target_rho.tsv, family_sc_summary.tsv}` |
| `brca_deconvolution` | `brca_deconvolution.py` | **Roadmap Phase 0: TCGA cell-type deconvolution (MH-58).** Streams the GSE176078 scRNA mtx → 9-type pseudobulk signature; NNLS-deconvolves TCGA bulk → per-sample fractions; validates vs marker-metagenes. Finding: immune ρ=0.85 / CAF ρ=0.57 usable, epithelial ρ=0.39 unreliable (needs CIBERSORTx). Feeds Phase 1-refined/2/3 (CAF/immune fractions) | GSE176078 scRNA (`data/external/brca_scrna_gse176078/`), TCGA RNA | `output/brca_deconvolution/{tcga_celltype_fractions.tsv, method_manifest.json}` |
| `program_pressure_by_role` | `program_pressure_by_role.py` | **Program pressure aggregated by gene-role (MH-57b).** Joins framework per-gene pressure (`gene_pressure_by_state`) to Hallmark membership + TF (Lambert2018) + high-evidence-target flags; per-program median pressure-delta stratified by role + global TF-vs-nonTF test. Answers "which programs brake-release, and is it on the drivers/TFs?" | `gene_pressure_by_state.tsv`, edge table, `annotations/humantfs_lambert2018_tf.tsv` | `output/program_pressure_by_role/{program_pressure_by_role.tsv, method_manifest.json}` |
| `core_coupling_composition_retest` | `core_coupling_composition_retest.py` | **Roadmap Phase 1: composition-retest of the core framework's headline couplings (MH-57a).** TCGA; headline BY-neg edges, partial Spearman CPE+HRD vs +epi/immune/stroma/proliferation metagenes; `--max-edges` caps the batch. Finding: 96% survive composition; ECM/EMT/stromal attenuated. Metagene route (scRNA-deconvolution = refined Phase 0/1) | TCGA rna/mirna/clinical, `coupling_permutation/coupling_edge.tsv.gz`, TF/markers | `output/core_coupling_composition_retest/{headline_edges_composition_retest.tsv, method_manifest.json}` |
| `eval.decoy_bench` | `eval/decoy_bench.py` | **⭐ THE DECOY BENCH (MH-135) — the canonical site-free negative control for the learned model.** Is the curated edge set better than a matched set of arms that CANNOT bind? Estimand: the OOF learned budget `M_g = Σ_f β_f·X_f` (β fitted out-of-fold over patients; β, z-params, variance floor and C-coefs all fit on TRAIN, applied to held-out) vs a decoy design. **Decoy = site-free (no TargetScan site, no scanMiR duplex, ⭐ no 6mer), not a seed-family or genomic-cluster mate, decorrelated in the SPARSE residual (|r_S|<0.30 — NOT raw |r|, which rejects high-loading arms and caused a ~2× overstatement), matched by optimal assignment on SIGNED loadings + dose + variance** (⭐ SIGNED: |loading| matching left 39% of decoys ANTI-matched on the purity axis). **Always runs BOTH C blocks and reports `retention = gap_deconv/gap_core` (axiom 2a).** Finding: core gap **−0.0306 (p=1.3e-18)**, deconv **−0.0205 (p=1.3e-12)**, **retention 0.67** ⇒ ⅔ cell-intrinsic; monotone in family count. ⚠ dose-corrected (Δdose=0) the gap is **−0.0175**. **⭐ Sequence-based positive control: genes whose curated edges are ALL seedless show REAL ρ −0.003 and gap +0.0006 — exactly zero, as they must.** Replaces ~40 scratchpad scripts; smoke-tested (`tests/test_decoy_bench.py`, 15 checks incl. a shuffled-Y leakage test) | `learned/{data,families,spike_slab,confounders,instrument}`, TargetScan context++, `data/external_cache/scanmir/genomewide_{kd*,3utr.fa}`, miRTarBase HE edges | `output/learned/{decoy_bench.tsv, sixmer_sites.tsv.gz}` |
| `eval.lit_ground_truth` | `eval/lit_ground_truth.py` | **⭐ THE VERSIONED CANONICAL-REGULATOR SET (MH-196) — the attribution answer key, made reproducible.** Replaces **five** producer-less hand-curated lists (n=16/19/21/23/173, all `/tmp` scratchpad, 2026-07-13) plus MH-126c's n=32, **which is not on disk at all**. Rule: canonical family of a gene = argmax of **distinct low-throughput functional PMIDs** (`reporter`/`western`/`proteomics`, weak excluded) from the PMID-deduped ledger — deliberately NOT `qpcr_rna` (swallows high-throughput arrays) and NOT `ago_clip`/`chimeric` (binding ≠ repression, and those are the discovery lane's INDEPENDENT evidence). Admitted only if the gene is in the design, the family is in THAT gene's design, ≥2 families compete, and the top is unambiguous (margin ≥1). **tier2 329 genes / 92 families · tier3 200/63 · tier4 132/48.** sha256 + source-file stamp in `lit_ground_truth_provenance.json`. ✅ Positive control: recovers PTEN→miR-21, ZEB1→miR-200, BCL2→miR-15/16, CDKN1A→miR-17/20/93 with no hand curation. ⚠ study bias is NOT removed by size — it makes ABUNDANCE the baseline to beat (see `attribution_rank`) | `learned/evidence/ledger.py`, `learned/families.py`, `output/learned/family_card.tsv` | `output/learned/{lit_ground_truth.tsv, lit_ground_truth_provenance.json}` |
| `eval.attribution_rank` | `eval/attribution_rank.py` | **⭐⭐ THE ATTRIBUTION TEST + THE FAMILY-FAME NULL (MH-196).** Where does the canonical family rank among a gene's design families, per readout? Normalised 0 = first, 0.5 = chance; **a RANK, never an argmax** (Design §F: same-seed arms are indistinguishable, so top-1 grades a distinction the data cannot support). Scores `identity`/`beta`/`beta_frac`/`*_deconv`/`abundance`; **REFUSES `prior_pi`/`pip_discovery` in code** (w-contaminated against a w-derived key — the gate is verified in `readouts.py:190-202`, where `wf` is dead and the dense chain runs at π≡1). **Result: β 0.436 p=0.021 — NO LONGER at chance (MH-126c's 0.518 is outside the CI); identity 0.416; abundance 0.359.** ⭐ **The FAME NULL is the point:** substituting another gene's canonical family that also sits in this design holds fame constant ⇒ **β Δ=−0.085 p=0.038 ✅ vs abundance Δ=−0.052 p=0.40 ✗** — abundance's raw win is largely publication bias (study-bias channel +0.187 within gene). Head-to-head **TIED** and **unresolvable in principle** (needs ~1,241 clusters vs a ~330-family literature ceiling). ⛔ **Carries a DEFINITIONAL harness control (`lit_depth`) that must rank first for EVERY gene — it caught 3 bugs that had inverted the abundance verdict** (`arms` is `;`-separated; card names lack the matrix `.N` suffix; NaN was ranked LAST at 2.7× worse canonical coverage) | `eval/lit_ground_truth.py`, `output/learned/family_card.tsv`, `learned/{data,families}`, `learned/evidence/ledger.py` | `output/learned/{attribution_rank.tsv, attribution_rank_summary.tsv, attribution_fame_null.tsv}` |
| `eval.discovery_gold_set` | `eval/discovery_gold_set.py` | **⭐ THE DISCOVERY LANE'S DELIVERABLE, WITH A PRODUCER (MH-198).** The A1∩site-Manakov convergent-evidence table — **157 edges · 90 genes · 18 arms · ⭐ 11 SEED FAMILIES**. Three independent hurdles: bulk coupling beyond the curated HE aggregate · composition-robust + standard-BH-passing (`fall_reason==A1_BY_multiplicity`) · **the predicted 3'UTR site physically COINCIDES with a Manakov chimeric duplex** (`site_manakov`), not merely a duplex somewhere on the gene. ⚠⚠ **THE CAVEAT TRAVELS WITH THE ARTIFACT: 96/157 (61%) are ONE seed family and 139/157 (88%) are the CO-TRANSCRIBED miR-17~92 polycistron + paralogue clusters** ⇒ ~one oncogenic cluster's realized target set, **not 157 independent findings — quote the 11 families beside the 157 edges.** ✅ Strong: **94/157 on genes with NO curated regulator at all**; only 3/157 same-seed paralogues of an already-curated regulator. ⛔ Corrects HANDOFF §6: `KIF3B` is edge-level CLIP only ⇒ the miR-18a flagship is a **TRIO** at site level; concept-level MWU re-derives to **p=2.28e−27** (full table) vs the recorded 1.9e−20 (narrower scope). ⚠ **Per-edge FDR is genuinely empty — a DIFFERENT statement from 'discovery is empty'.** ⬜ The registry FINDING is deliberately unwritten pending the user's framing decision (HANDOFF §7.3) | `output/learned/{discovery_fall_diagnosis,discoveries_sitelevel}.tsv` (from `learned/discovery.py`, `method_dev/site_ladder/discovery_site_evidence.py`) | `output/learned/{discovery_gold_set.tsv, discovery_gold_set_provenance.json}` |
| `learned.arm_card` | `learned/arm_card.py` | **⭐ THE ARM RUNG — the FOURTH card, and the one home for every per-ARM property (MH-209).** Until 2026-08-03 `card_rungs` knew only `edge`/`family`/`gene`, so per-arm facts had nowhere to live and each module re-derived its own (`genomewide_promiscuity`, `kd`, `genomic_context`, `ago_loading`, `rarity_bench`, `arm_rung`) — **which is how MH-208's promiscuity annotation stayed mis-specified for five weeks with one consumer.** Emits **3,241 arms × 47 cols**. ⭐ **Every column carries a PROVENANCE PREFIX because the three targetome universes are different objects:** `seq_` genome-wide scanMiR K_D over 16,761 breast-expressed genes (746 arms) · `site_` scanMiR site-type counts ⚠ **HALLMARK-SCOPED, 1,432 genes** (771) · `ts_` TargetScan per-SITE context++/KD (⚠ **321** arms) · `cur_`/`fame_` ⛔ **FAME axes — carried to CONTROL for study bias, never as breadth** (curated degree ↔ #PMIDs ρ=+0.736) · `abund_`, `model_`, host/AGO · four `cov_*` booleans — **missing = UNSCANNED, never zero; nothing imputed.** Coverage on the 803 MODEL arms: site 96.0% · cur 99.0% · seq 65.8% · ts 33.7%. ✅ **Definitional control: re-derives all five MH-208 correlations to the digit.** ⚠ Site-type coding **verified** (context++ monotone −0.253/−0.169/−0.127), not assumed. ⚠ Two design assumptions the data refuted, recorded in the docstring: sites-vs-genes is **not** an independent axis (ρ=+0.927, range 1.00–2.88), and the **8mer share is TOOL-DEPENDENT** (scanMiR vs TargetScan ρ=+0.620, medians 2.2× apart) | `data/miRNA/Predicted_Targets_Context_Scores.default_predictions.txt`, `data/external_cache/scanmir/{kd_repression,genomewide_kd*}.tsv.gz`, `output/genomewide_promiscuity/*`, ledger, TCGA X | `output/learned/arm_card.tsv`; CLI `.venv/bin/python3 -m mirna_hallmark.learned.arm_card [--rebuild]` |
| `learned.gene_axes` | `learned/gene_axes.py` | **⭐ THE REUSABLE MULTI-AXIS GENE CHARACTERISATION (user-directed, generalised out of MH-201).** For ANY per-gene outcome: `build`/`regulator_axes`/`self_axes`/`weight_axes` assemble ~100 axes; `scan` FDR-tests every axis against the outcome; `contrast` profiles the extreme genes; **`sign_analysis` asks whether a gain is SIGN CORRECTION rather than sharpening** — the question that turned MH-201's modest margin into a claim (reference correct 51.1% = chance vs candidate 58.5%; net +49 rescued; and the correction ASYMMETRIC — moves wrong answers p=1.5e-16, moves right answers by exactly 0.0000). ⚠⚠ **Encodes two traps that cost real errors:** ① **`mask_degenerate`** — with ONE unit a weighted estimator is IDENTICAL to the unweighted one under Spearman (43% of MH-201's universe; pooling produced a nonsense 'Δ=+0.0000, p=1.3e-27'); ② **`hhi(normalise=True)`** — raw HHI is bounded below by 1/k so `corr(k, HHI)` is negative whatever the data does (**−0.667 raw vs −0.075 corrected**, axiom 5). ⭐ Doctrine it carries: the REGULATOR-ensemble axes are usually strongest (`reg_dose_hhi` q=2e-05 — a weighted estimator cannot beat an unweighted sum when one member dominates the abundance); DYNAMIC RANGE predicts where MEAN level does not; weight-shape axes are inert; Shapley enters as COVERAGE not shape | any per-gene outcome + a cohort expression matrix; `output/learned/realization/gene_card.tsv`, `family_card.tsv` | — (library) |
| `learned.eval.ood_cohort` | `learned/eval/ood_cohort.py` | **⭐⭐ THE COHORT-BOUNDARY TRANSFER TEST (MH-201, upgraded MH-204) — the model's only untested boundary.** β **FROZEN** on TCGA, **zero parameters fit in Buffa** (GSE22216 miRNA + GSE22219 mRNA, **n=207**, Oxford 1989–92). **Rung = FAMILY, forced** — Buffa's array is 501 legacy bare-stem probes with no -5p/-3p resolution. ⭐ **THE RESULT IS A DISSOCIATION: LEVEL transports (−0.0186, 58.5% repression-directed, retention +0.358 against the C-ablation ceiling), RANK does NOT separate β from abundance (+0.222 vs +0.183, increment CI spans 0).** ⭐⭐ **The C-ABLATION CEILING is the design's load-bearing piece** — the TCGA ceiling recomputed under the *exact reduced C Buffa can support*, because otherwise "cohort boundary" is confounded with "weaker confounder block" (dropping `target_cn`+CPE alone costs **33%** of TCGA \|ρ\|). **Every retention figure names its denominator.** ⭐ `gene_alias()` — without it **16.7% of the β universe is silently dropped including ZEB1** (the array calls it `TCF8`); 73 genes recovered from GPL6098's own `Synonym` column. ⛔ **`target_cn` is UNOBTAINABLE, not absent** — GSE22220 has exactly two subseries, Buffa 2011 generated no CNV, and **Buffa is NOT a METABRIC subset** (0/207 match at r>0.90; Oxford vs Cambridge/Vancouver). **Do not repeat that search.** | `eval/buffa_validation.py`, `output/learned/family_card.tsv`, `decoy_family_betas_oof.tsv`, `estimate_scores.py`, `spatial_common.deconvolve_matrix` | `output/learned/ood_cohort/{ood_cohort_genes.tsv, ood_cohort_manifest.json, ood_cohort_modifier_scan.tsv, ood_cohort_regulator_features.tsv, r2a_decoy_oof.tsv, edge_leg_{raw,metagene,nnls}.tsv}` |
| `learned.eval.beta_prognostic` | `learned/eval/beta_prognostic.py` | **THE LEARNED-β OUTCOME ARM (MH-202) — does a coupling model earn its weights on survival?** Buffa DRFS (77 events) + TCGA OS/DFI. **Verdict: per-gene YES, aggregate NO, and the WEIGHTS earn nothing** — β-weighted budgets do not beat unweighted abundance on outcome, consistent with Axis 7's standing "pressure magnitude is non-prognostic". ✅ **Carries a hard positive-control GATE (`gate()`): miR-210→DRFS must reproduce Buffa 2011, and the module REFUSES to report if it fails** — which is what separates a credible null from an underpowered one. ⚠ `_tcga_one()` reports in-sample **and** OOF side by side; TCGA is the **weaker** cohort here (OS only, no DRFS). | `eval/buffa_validation.py`, `output/learned/family_card.tsv`, TCGA clinical | `output/learned/ood_cohort/{beta_prognostic_genes.tsv, beta_prognostic_tcga.tsv, beta_prognostic_manifest.json}` |
| `learned.eval.rppa_protein` | `learned/eval/rppa_protein.py` | **⭐⭐ THE RPPA PROTEIN CHANNEL AT n=866 (MH-203) — opened for the first time; the subproject had ZERO references to `data/rppa/`.** 881×461 participant×antibody, **866 also have miRNA — the SAME patients, so no cohort boundary.** ⭐ **The power argument is the point:** detectable \|ρ\| at 80% power is **0.276 at CPTAC's n=101** vs **0.095 here**, against a typical coupling of **0.07–0.12** ⇒ **CPTAC's floor was ~3× the effect.** ⚠ Does **not** rescue MH-103 (that died of a *mediator leak* — a design flaw, not power). ⭐ **THE DECOMPOSITION THE PROJECT NEVER HAD n FOR:** `rho_prot` / `rho_mrna` / **`rho_disc`** (protein residualised on its own mRNA = the TRANSLATIONAL channel). **Result: destabilisation, NOT translation** — the signal is in `rho_mrna`, not `rho_disc`. Emitted under **both** C blocks with retention (axiom 2a — this is exactly where CPTAC was burned). ✅ Positive control = MH-196's literature set ∩ RPPA = **104 genes (77 at ≥3 PMIDs)**. ⚠ Phospho probes measure a *modified fraction*, flagged and never pooled with total-protein probes; Fisher-z uses **df = n−3−k**, k∈{3,11}. | `analysis/utils/rppa_antibody_matrix.py`, `output/learned/family_card.tsv`, `learned/{data,families,attribution_eb}` | `output/learned/rppa/{rppa_coupling.tsv, gap_axes_oof.tsv, gap_axes_oof_genes.tsv}` |
| `eval.decoy_oof_budgets` | `eval/decoy_oof_budgets.py` | **⭐⭐ OOF REAL+DECOY BUDGETS ON THE CURRENT PAIRS (MH-204) — the artifact the decoy arc lacked, built after the user asked *"why are there so few n?"*.** `decoy_bench` persists a per-gene ρ but **never the per-sample BUDGET**, so anything scoring a decoy against another layer (protein) or inside a stratum (PAM50) fell back on **`decoy_family_betas.tsv` (2026-07-13) — which overlaps the CURRENT matched pairs by 63/3,558 cells = 1.8%, covers 516 of 1,395 genes, and predates MH-137's three fixes** ⇒ its decoys are **too WEAK and flatter real**. The "few n" was never power; it was a superseded artifact's footprint. **Emits BOTH the OOF budgets and `decoy_family_betas_oof.tsv` (4,889 cells / 1,395 genes) as a drop-in replacement** — same schema, current pairs, full-data β because a *transported* decoy must be a frozen weight vector. ⚠ **Per-fold standardisation before pooling (MH-181)** — `Z·β` is arbitrary-scale under a per-fold-refit β. ⭐ **Consequence: it flipped MH-201's R2a from `p=0.335 "underpowered"` to `p=3.5e-06`.** The superseded tables are archived at `ARCHIVE_PRE_MH204/` with a replacement map. | `output/learned/decoy_full_pairs.tsv`, `learned/{data,families,spike_slab}` | `output/learned/{decoy_oof_budgets.parquet, decoy_family_betas_oof.tsv, decoy_oof_scored.tsv}` |
| `learned.eval.layer_boundary` | `learned/eval/layer_boundary.py` | **⭐⭐ IS THE PROTEIN NULL *BIOLOGY* OR *INSTRUMENT*? (MH-205) — the module that WITHDREW MH-203/204's "layer split".** MH-203/204 compared a protein gap on **180** RPPA genes against an mRNA gap on **1,354** and read the difference as a LAYER boundary; **gene selection, power and layer were confounded in that one number.** ⭐ **Stage `rppa` needs no new data and is the whole reframe: on the SAME 180 genes the *mRNA* gap is −0.0103, p=0.0565 — also n.s. Matched retention 84%, not the naive 45%. MDE at n=180 is 0.0309/0.0232 ⇒ both observed values sit INSIDE their own noise floor.** Not phospho either (13/180; dropping them: −0.0086→−0.0089). ⭐⭐ **Stages `tcga105`/`prospective` supply the decisive test — PAIRED ON GENE (selection cancels) WITHIN a cohort (cohort cancels), at 5× the gene count: +0.0018 (p=0.287, n=849) and +0.0056 (p=0.341, n=897) ⇒ the layers are INDISTINGUISHABLE.** ⚠ **`tcga105` is chosen *because* it is patient-circular** — sharing patients with the fit removes the cohort boundary, so what `ood_protein.py` flags as its weakness is this design's requirement. ⚠ Every cell prints its own **MDE** (`<80% power` ≠ null: the signed-rank threshold is 0.70×MDE) and both C blocks with retention. ⭐ Carries a **denominator gate** after an ungated retention printed −3.53 on a −0.0003 denominator. ✅ Destabilisation-not-translation survives on 849/897 genes. | `output/learned/{decoy_oof_scored.tsv, decoy_oof_budgets.parquet, family_card.tsv, decoy_family_betas_oof.tsv, decoy_full_pairs.tsv}`, `eval/cptac_validation.py`, `learned/confounders.py` | `output/learned/layer_boundary/{tcga105_gaps.tsv, prospective_gaps.tsv, layer_boundary_report.json}` |
| `run_all` | `run_all.py` | orchestrate all steps | — | `output/run_manifest.json` |

## Shared modules (not standalone analyses)

| module | role |
|--------|------|
| `config.py` | paths, `STRATUM_SPECS`, `AgoGateParams`, evidence/pressure cutoffs, FDR α |
| `data_loaders.py` | clinical strata, miRNA arms, RNA, AGO/RISC expr, target-gene CNV (cached), helpers |
| `stats.py` | `bh_fdr`, `kruskal_across_strata`, `hypergeom_enrichment`, `zscore_rows` |
| `learned/regression.py` | Gene-focused non-negative **adaptive-lasso** (§3a Phase-1). ⚠ **§6b-RETIRED** — kept as a baseline; the canonical estimator is the Gibbs (`spike_slab`). |
| `learned/priors.py` | Per-edge priors π (inclusion), μ (magnitude), τ (confidence) — the Design §D/E prior block the posterior consumes. |
| `learned/attribution_eb.py` | EB posterior vs bagged-NNLS for **attribution** (per-gene learned-τ²); the `_prep` z-scored/C-residualised design + the identity-vs-magnitude split. |
| `learned/seq_specificity.py` | Per-edge **sequence specificity** = affinity CONCENTRATION (validated replacement for seed-count); feeds the inclusion prior. |
| `learned/seed_rarity.py` | **Seed-rarity** specificity — the collinearity-immune resolver of who-owns-a-gene. |
| `learned/rarity_bench.py` | Convergence bench for the specificity/rarity term — WHERE seed-rarity enters the final Bayes model (also the `_prep` used by the genome sweeps). |
| `learned/chimeric_evidence.py` | Formalized **chimeric** (miRNA↔target ligation) evidence — the arm-resolving source (Manakov eCLIP + TarBase CLASH/qCLASH). ⚠ no breast chimeric exists. |
| `learned/structural_identity.py` | **Structural identity** — the LOSS lens (precursor identity; Design §I). |
| `learned/cptac_data.py` | CPTAC prospective data assembly for the **protein** axis — the CPTAC twin of `learned/data.py`. |
| `learned/realization.py` (+ driver `analyses/progression/realize_run.py`) | **⭐ PROGRESSION — within-patient paired NAT→tumour REALIZATION engine (MH-158)**, the one progression estimator surviving the dead cross-cohort channel (differencing removes the patient baseline). M = leak-free `M_complement` (non-matched tumours). Multi-resolution: gene/edge (`realize_gene_edge`), family (`realize_family`, nonlinear pooled Δ), between-family Shapley + **owner-convergence** (`realize_between_family`/`owner_convergence`), within-family (`realize_within_family`, prereg null); DOSE axis (`dose_shift_arm/edge`, `own_specific_frac`); site-free orientation-stratified decoy null (`realize_null`); NAT-reference adjudication + decoy control (`nat_reference_adjudication`/`nat_decoy_control`/`decoy_stratify`); descriptive layer (`mirna_nat_retention`/`patient_nat_identity`); Phase-2/3 joins (`retention_realization`/`context_hallmark_rollup`/`own_retaining_clinical`); **⭐ integrated cards `progression_edge_card` (one row/edge — cross-state shift_class+coupling+grank+arm_lfc ⋈ within-patient paired edge_rho_adj/own_specific_frac/dShare/family realization_identity/is_owner; supersedes `master_edge_patterns`) + `progression_gene_card` (one row/gene — attribution ⋈ gene+family-pooled realization ⋈ acquired dose ⋈ net-repression ⋈ realization/static OWNER+owner_agrees ⋈ REGULATORY-HANDOFF dominant-per-state)**. Sharded CLI: `realize_run.py --drive` (Phase 1) / `--drive-phase2` (family+between-family+cards) / `--decoy-control`; subprocess shards + single-thread BLAS (axiom 3a). Outputs `output/learned/realization/*.tsv`. |
| `analyses/progression/landscape_characterization.py` | **PROGRESSION LANDSCAPE biological characterization** (descriptive map, not an estimand test). Reads `progression_edge_card` + `mirna_state_class` + `genomic_context`; emits four threads — convergence hubs (PTEN 28-edge supersink), cluster-as-unit (oncomiR polycistrons acquire / 14q32 + miR-30 + let-7 lose as co-transcribed units), subtype trajectories (Basal-concentrated), driver/loser rosters (named acquired-oncomiR→TSG / silenced-TSG-miR→oncogene). ⚠ DESCRIPTIVE (shift_class FDR caveat; roles incomplete); home BIOLOGICAL_SYNTHESIS §12. CLI: `python -m mirna_hallmark.analyses.progression.landscape_characterization`. Outputs `output/learned/realization/landscape_*.tsv`. |
| `analyses/progression/dominance_trajectory.py` | **PER-TARGET realized-dominance trajectory (MH-166 follow-up).** For each edge (arm→gene), WHEN across GTEx→NAT→TUM the arm becomes a top-K realized repressor of THAT gene, via per-state CALIBRATED coupling — the GTEx (hly) node reads `coupling_p_hly_resolved` (direct where measured, same-seed SURROGATE where collapsed, BLIND otherwise); nat/tum uncollapsed ⇒ all arms. Emits `dominance_timing` ∈ {constitutive_dom, field_acquired_dom (by NAT), malignant_acquired_dom (NAT→TUM), …, hly-blind}. Finding: per-target dominance is predominantly a MALIGNANT-step event (522 vs 48 field vs 50 constitutive of 620 clean). Reads `progression_edge_card`; CLI `python -m mirna_hallmark.analyses.progression.dominance_trajectory`. Output `output/learned/realization/dominance_trajectory.tsv`. |

## Flags worth knowing

- `build_edges --force-rebuild` / `--use-canonical-builder`
- `ago_gate --include-tnrc6`
- `mirna_locus_cnv --force-rebuild` (re-extracts cohort CNV; ~12 min)
- `mirna_cnv_genome_maps` (reuses cached miRNA CNV; writes maps and flags)
- `mirna_cnv_subtype_depth` (reuses cached miRNA CNV; writes focus-arm distributions and neighborhoods)
- `target_combined_anticorr` (reuses existing pressure builder; computes target-gene combined anti-correlation)
- `family_normal_reference --skip-gtex` (NAT-only; skip GTEx healthy reference). GTEx files live in `data/GTEx/` (downloaded from the GTEx Portal V10 open-access bucket: `miRNA_TPM_matrix_PORTAL_2025_03_17.txt.gz`, `gene_tpm_v10_breast_mammary_tissue.gct.gz`, `smallRNA.filtered_annotated_031725.txt`, `GTEx_Analysis_v10_Annotations_SampleAttributesDS.txt`).
- `stratum_characterization --all-edges` (don't restrict to high-evidence)
- `hallmark_interaction --include-tnrc6`
- `subtype_contrasts --include-tnrc6`
- `pam50_gene_resolution --include-tnrc6`
- `run_all --skip-mirna-cnv --force-edges --include-tnrc6`

## Caches

| cache | built by | rebuild |
|-------|----------|---------|
| `output/matrices/cnv_target_genes.tsv.gz` | `data_loaders.load_cnv_target_genes` | delete file |
| `output/matrices/cnv_ago_risc.tsv.gz` | `ago_gate` CNV summary | delete file |
| `output/mirna_locus_cnv/tables/sample_entity_cnv.tsv.gz` | `mirna_locus_cnv` | `--force-rebuild` |
