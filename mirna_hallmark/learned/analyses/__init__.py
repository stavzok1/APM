"""One-off / CLI analyses over the learned model (relocated from learned/ 2026-07-17, MH-155).
These import the model spine but nothing imports them — they produce a finding and are cited by a registry
row. Kept out of the model package proper so learned/ is the spine + evidence/eval, not a script drawer."""
